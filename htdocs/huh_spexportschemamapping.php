<?php
//==============================================================================
//===   spexportschemamapping.php                         
//===   Autogenerated by Druid from MySQL db Build:6
//==============================================================================

include_once("druid_interfaces.php");

class huh_spexportschemamapping implements  model , loadableModel, saveableModel, tableSchema 
{
   // These constants hold the sizes the fields in this table in the database.
   const SPEXPORTSCHEMAMAPPINGID_SIZE = 11; //INTEGER
   const TIMESTAMPCREATED_SIZE = 21; //TIMESTAMP
   const TIMESTAMPMODIFIED_SIZE = 21; //TIMESTAMP
   const VERSION_SIZE         = 11; //INTEGER
   const DESCRIPTION_SIZE     = 255; //255
   const MAPPINGNAME_SIZE     = 20; //YEAR
   const TIMESTAMPEXPORTED_SIZE = 21; //TIMESTAMP
   const CREATEDBYAGENTID_SIZE = 11; //INTEGER
   const MODIFIEDBYAGENTID_SIZE = 11; //INTEGER
    // These constants hold the field names of the table in the database. 
   const SPEXPORTSCHEMAMAPPINGID = 'SpExportSchemaMappingID';
   const TIMESTAMPCREATED  = 'TimestampCreated';
   const TIMESTAMPMODIFIED = 'TimestampModified';
   const VERSION           = 'Version';
   const DESCRIPTION       = 'Description';
   const MAPPINGNAME       = 'MappingName';
   const TIMESTAMPEXPORTED = 'TimeStampExported';
   const CREATEDBYAGENTID  = 'CreatedByAgentID';
   const MODIFIEDBYAGENTID = 'ModifiedByAgentID';

   //---------------------------------------------------------------------------

   // interface tableSchema implementation
   // schemaPK returns array of primary key field names
   public function schemaPK() {
       return $this->primaryKeyArray;
   } 
   // schemaHaveDistinct returns array of field names for which selectDistinct{fieldname} methods are available.
   public function schemaHaveDistinct() {
       return $this->selectDistinctFieldsArray;
   } 
   // schemaFields returns array of all field names
   public function schemaFields() { 
       return $this->allFieldsArray;
   } 
/*  Example sanitized retrieval of variable matching object variables from $_GET 
/*  Customize these to limit each variable to narrowest possible set of known good values. 

  $SpExportSchemaMappingID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['SpExportSchemaMappingID']), 0, 11);
  $TimestampCreated = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['TimestampCreated']), 0, 21);
  $TimestampModified = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['TimestampModified']), 0, 21);
  $Version = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['Version']), 0, 11);
  $Description = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['Description']), 0, 255);
  $MappingName = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['MappingName']), 0, 20);
  $TimeStampExported = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['TimeStampExported']), 0, 21);
  $CreatedByAgentID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['CreatedByAgentID']), 0, 11);
  $ModifiedByAgentID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['ModifiedByAgentID']), 0, 11);
*/

   //---------------------------------------------------------------------------

   private $SpExportSchemaMappingID; // PK INTEGER 
   private $TimestampCreated; // TIMESTAMP 
   private $TimestampModified; // TIMESTAMP 
   private $Version; // INTEGER 
   private $Description; // VARCHAR(255) 
   private $MappingName; // YEAR 
   private $TimeStampExported; // TIMESTAMP 
   private $CreatedByAgentID; // INTEGER 
   private $ModifiedByAgentID; // INTEGER 
   private $dirty;
   private $loaded;
   private $error;
   const FIELDLIST = ' SpExportSchemaMappingID, TimestampCreated, TimestampModified, Version, Description, MappingName, TimeStampExported, CreatedByAgentID, ModifiedByAgentID, ';
   const PKFIELDLIST = ' SpExportSchemaMappingID, ';
   const NUMBER_OF_PRIMARY_KEYS = 1;
   private $primaryKeyArray = array( 1 => 'SpExportSchemaMappingID'  ) ;
   private $allFieldsArray = array( 0 => 'SpExportSchemaMappingID' , 1 => 'TimestampCreated' , 2 => 'TimestampModified' , 3 => 'Version' , 4 => 'Description' , 5 => 'MappingName' , 6 => 'TimeStampExported' , 7 => 'CreatedByAgentID' , 8 => 'ModifiedByAgentID'  ) ;
   private $selectDistinctFieldsArray = array( 7 => 'CreatedByAgentID' , 8 => 'ModifiedByAgentID'  ) ;

   //---------------------------------------------------------------------------

   // constructor 
   function huh_spexportschemamapping(){
       $this->SpExportSchemaMappingID = NULL;
       $this->TimestampCreated = '';
       $this->TimestampModified = '';
       $this->Version = '';
       $this->Description = '';
       $this->MappingName = '';
       $this->TimeStampExported = '';
       $this->CreatedByAgentID = '';
       $this->ModifiedByAgentID = '';
       $this->dirty = false;
       $this->loaded = false;
       $this->error = '';
   }

   private function l_addslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = addslashes($value);
      }
      return $retval;
   }
   private function l_stripslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = stripslashes($value);
      }
      return $retval;
   }
   public function isDirty() {
       return $this->dirty;
   }
   public function isLoaded() {
       return $this->loaded;
   }
   public function errorMessage() {
       return $this->error;
   }

   //---------------------------------------------------------------------------

   public function keyValueSet($fieldname,$value) {
       $returnvalue = false;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpExportSchemaMappingID') { $returnvalue = $this->setSpExportSchemaMappingID($value); } 
             if ($fieldname=='TimestampCreated') { $returnvalue = $this->setTimestampCreated($value); } 
             if ($fieldname=='TimestampModified') { $returnvalue = $this->setTimestampModified($value); } 
             if ($fieldname=='Version') { $returnvalue = $this->setVersion($value); } 
             if ($fieldname=='Description') { $returnvalue = $this->setDescription($value); } 
             if ($fieldname=='MappingName') { $returnvalue = $this->setMappingName($value); } 
             if ($fieldname=='TimeStampExported') { $returnvalue = $this->setTimeStampExported($value); } 
             if ($fieldname=='CreatedByAgentID') { $returnvalue = $this->setCreatedByAgentID($value); } 
             if ($fieldname=='ModifiedByAgentID') { $returnvalue = $this->setModifiedByAgentID($value); } 
             $returnvalue = true;
          }
          catch (exception $e) { ;
              $returnvalue = false;
              throw new Exception('Field Set Error'.$e->getMessage()); 
          }
       } else { 
          throw new Exception('No Such field'); 
       }  
       return $returnvalue;
   }
   public function keyGet($fieldname) {
       $returnvalue = null;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpExportSchemaMappingID') { $returnvalue = $this->getSpExportSchemaMappingID(); } 
             if ($fieldname=='TimestampCreated') { $returnvalue = $this->getTimestampCreated(); } 
             if ($fieldname=='TimestampModified') { $returnvalue = $this->getTimestampModified(); } 
             if ($fieldname=='Version') { $returnvalue = $this->getVersion(); } 
             if ($fieldname=='Description') { $returnvalue = $this->getDescription(); } 
             if ($fieldname=='MappingName') { $returnvalue = $this->getMappingName(); } 
             if ($fieldname=='TimeStampExported') { $returnvalue = $this->getTimeStampExported(); } 
             if ($fieldname=='CreatedByAgentID') { $returnvalue = $this->getCreatedByAgentID(); } 
             if ($fieldname=='ModifiedByAgentID') { $returnvalue = $this->getModifiedByAgentID(); } 
          }
          catch (exception $e) { ;
              $returnvalue = null;
          }
       }
       return $returnvalue;
   }
/*SpExportSchemaMappingID*/
   public function getSpExportSchemaMappingID() {
       if ($this->SpExportSchemaMappingID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SpExportSchemaMappingID));
       }
   }
   public function setSpExportSchemaMappingID($SpExportSchemaMappingID) {
       if (strlen(preg_replace('/[^0-9]/','',$SpExportSchemaMappingID)) > huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $SpExportSchemaMappingID = trim($SpExportSchemaMappingID);
       if (!ctype_digit(strval($SpExportSchemaMappingID)) && trim(strval($SpExportSchemaMappingID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->SpExportSchemaMappingID = $this->l_addslashes($SpExportSchemaMappingID);
       $this->dirty = true;
   }
/*TimestampCreated*/
   public function getTimestampCreated() {
       if ($this->TimestampCreated==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TimestampCreated));
       }
   }
   public function setTimestampCreated($TimestampCreated) {
       if (strlen($TimestampCreated) > huh_spexportschemamapping::TIMESTAMPCREATED_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->TimestampCreated = $this->l_addslashes($TimestampCreated);
       $this->dirty = true;
   }
/*TimestampModified*/
   public function getTimestampModified() {
       if ($this->TimestampModified==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TimestampModified));
       }
   }
   public function setTimestampModified($TimestampModified) {
       if (strlen($TimestampModified) > huh_spexportschemamapping::TIMESTAMPMODIFIED_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->TimestampModified = $this->l_addslashes($TimestampModified);
       $this->dirty = true;
   }
/*Version*/
   public function getVersion() {
       if ($this->Version==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->Version));
       }
   }
   public function setVersion($Version) {
       if (strlen(preg_replace('/[^0-9]/','',$Version)) > huh_spexportschemamapping::VERSION_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $Version = trim($Version);
       if (!ctype_digit(strval($Version)) && trim(strval($Version))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->Version = $this->l_addslashes($Version);
       $this->dirty = true;
   }
/*Description*/
   public function getDescription() {
       if ($this->Description==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->Description));
       }
   }
   public function setDescription($Description) {
       if (strlen($Description) > huh_spexportschemamapping::DESCRIPTION_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->Description = $this->l_addslashes($Description);
       $this->dirty = true;
   }
/*MappingName*/
   public function getMappingName() {
       if ($this->MappingName==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->MappingName));
       }
   }
   public function setMappingName($MappingName) {
       if (strlen($MappingName) > huh_spexportschemamapping::MAPPINGNAME_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->MappingName = $this->l_addslashes($MappingName);
       $this->dirty = true;
   }
/*TimeStampExported*/
   public function getTimeStampExported() {
       if ($this->TimeStampExported==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TimeStampExported));
       }
   }
   public function setTimeStampExported($TimeStampExported) {
       if (strlen($TimeStampExported) > huh_spexportschemamapping::TIMESTAMPEXPORTED_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->TimeStampExported = $this->l_addslashes($TimeStampExported);
       $this->dirty = true;
   }
/*CreatedByAgentID*/
   public function getCreatedByAgentID() {
       if ($this->CreatedByAgentID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->CreatedByAgentID));
       }
   }
   public function setCreatedByAgentID($CreatedByAgentID) {
       if (strlen(preg_replace('/[^0-9]/','',$CreatedByAgentID)) > huh_spexportschemamapping::CREATEDBYAGENTID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $CreatedByAgentID = trim($CreatedByAgentID);
       if (!ctype_digit(strval($CreatedByAgentID)) && trim(strval($CreatedByAgentID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->CreatedByAgentID = $this->l_addslashes($CreatedByAgentID);
       $this->dirty = true;
   }
/*ModifiedByAgentID*/
   public function getModifiedByAgentID() {
       if ($this->ModifiedByAgentID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->ModifiedByAgentID));
       }
   }
   public function setModifiedByAgentID($ModifiedByAgentID) {
       if (strlen(preg_replace('/[^0-9]/','',$ModifiedByAgentID)) > huh_spexportschemamapping::MODIFIEDBYAGENTID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $ModifiedByAgentID = trim($ModifiedByAgentID);
       if (!ctype_digit(strval($ModifiedByAgentID)) && trim(strval($ModifiedByAgentID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->ModifiedByAgentID = $this->l_addslashes($ModifiedByAgentID);
       $this->dirty = true;
   }
   public function PK() { // get value of primary key 
        $returnvalue = '';
        $returnvalue .= $this->getSpExportSchemaMappingID();
        return $returnvalue;
   }
   public function PKArray() { // get name and value of primary key fields 
        $returnvalue = array();
        $returnvalue['SpExportSchemaMappingID'] = $this->getSpExportSchemaMappingID();
        return $returnvalue;
   }
   public function NumberOfPrimaryKeyFields() { // returns the number of primary key fields defined for this table 
        return 1;
   }

   // Constants holding the mysqli field type character (s,i,d) for each field
  const C_SpExportSchemaMappingIDMYSQLI_TYPE = 'i';
  const C_TimestampCreatedMYSQLI_TYPE = 's';
  const C_TimestampModifiedMYSQLI_TYPE = 's';
  const C_VersionMYSQLI_TYPE = 'i';
  const C_DescriptionMYSQLI_TYPE = 's';
  const C_MappingNameMYSQLI_TYPE = 's';
  const C_TimeStampExportedMYSQLI_TYPE = 's';
  const C_CreatedByAgentIDMYSQLI_TYPE = 'i';
  const C_ModifiedByAgentIDMYSQLI_TYPE = 'i';

   // function to obtain the mysqli field type character from a fieldname
   public function MySQLiFieldType($aFieldname) { 
      $retval = '';
      if ($aFieldname=='SpExportSchemaMappingID') { $retval = self::C_SpExportSchemaMappingIDMYSQLI_TYPE; }
      if ($aFieldname=='TimestampCreated') { $retval = self::C_TimestampCreatedMYSQLI_TYPE; }
      if ($aFieldname=='TimestampModified') { $retval = self::C_TimestampModifiedMYSQLI_TYPE; }
      if ($aFieldname=='Version') { $retval = self::C_VersionMYSQLI_TYPE; }
      if ($aFieldname=='Description') { $retval = self::C_DescriptionMYSQLI_TYPE; }
      if ($aFieldname=='MappingName') { $retval = self::C_MappingNameMYSQLI_TYPE; }
      if ($aFieldname=='TimeStampExported') { $retval = self::C_TimeStampExportedMYSQLI_TYPE; }
      if ($aFieldname=='CreatedByAgentID') { $retval = self::C_CreatedByAgentIDMYSQLI_TYPE; }
      if ($aFieldname=='ModifiedByAgentID') { $retval = self::C_ModifiedByAgentIDMYSQLI_TYPE; }
      return $retval;
   }

   // Function load() can take either the value of the primary key which uniquely identifies a particular row
   // or an array of array('primarykeyfieldname'=>'value') in the case of a single field primary key
   // or an array of fieldname value pairs in the case of multiple field primary key.
   public function load($pk) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        try {
             if (is_array($pk)) { 
                 $this->setSpExportSchemaMappingID($pk[SpExportSchemaMappingID]);
             } else { ;
                 $this->setSpExportSchemaMappingID($pk);
             };
        } 
        catch (Exception $e) { 
             throw new Exception($e->getMessage());
        }
        if($this->SpExportSchemaMappingID != NULL) {
           $sql = 'SELECT SpExportSchemaMappingID, TimestampCreated, TimestampModified, Version, Description, MappingName, TimeStampExported, CreatedByAgentID, ModifiedByAgentID FROM spexportschemamapping WHERE SpExportSchemaMappingID = '.$this->SpExportSchemaMappingID ;

           $preparesql = 'SELECT SpExportSchemaMappingID, TimestampCreated, TimestampModified, Version, Description, MappingName, TimeStampExported, CreatedByAgentID, ModifiedByAgentID FROM spexportschemamapping WHERE SpExportSchemaMappingID = ? ';

           if ($statement = $connection->prepare($preparesql)) { 
              $statement->bind_param("i", $this->SpExportSchemaMappingID);
              $statement->execute();
              $statement->bind_result($this->SpExportSchemaMappingID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->Description, $this->MappingName, $this->TimeStampExported, $this->CreatedByAgentID, $this->ModifiedByAgentID);
              $statement->fetch();
              $statement->close();
           }

            $this->loaded = true;
            $this->dirty = false;
        } else { 
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   // Function save() will either save the current record or insert a new record.
   // Inserts new record if the primary key field in this table is null 
   // for this instance of this object.
   // Otherwise updates the record identified by the primary key value.
   public function save() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        // Test to see if this is an insert or update.
        if ($this->SpExportSchemaMappingID!= NULL) {
            $sql  = 'UPDATE  spexportschemamapping SET ';
            $isInsert = false;
            $sql .=  "TimestampCreated = ? ";
            $sql .=  ", TimestampModified = ? ";
            $sql .=  ", Version = ? ";
            $sql .=  ", Description = ? ";
            $sql .=  ", MappingName = ? ";
            $sql .=  ", TimeStampExported = ? ";
            $sql .=  ", CreatedByAgentID = ? ";
            $sql .=  ", ModifiedByAgentID = ? ";

            $sql .= "  WHERE SpExportSchemaMappingID = ? ";
        } else {
            $sql  = 'INSERT INTO spexportschemamapping ';
            $isInsert = true;
if ($this->PK==NULL) { throw new Exception('Can\'t insert record with null primary key for this table'); }
            $sql .= '( SpExportSchemaMappingID ,  TimestampCreated ,  TimestampModified ,  Version ,  Description ,  MappingName ,  TimeStampExported ,  CreatedByAgentID ,  ModifiedByAgentID ) VALUES (';
            $sql .=  "  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .= ')';

        }
        if ($statement = $connection->prepare($sql)) { 
           if ($this->SpExportSchemaMappingID!= NULL ) {
              $statement->bind_param("ississsiii", $this->SpExportSchemaMappingID , $this->TimestampCreated , $this->TimestampModified , $this->Version , $this->Description , $this->MappingName , $this->TimeStampExported , $this->CreatedByAgentID , $this->ModifiedByAgentID , $this->SpExportSchemaMappingID );
           } else { 
              $statement->bind_param("ississsii", $this->SpExportSchemaMappingID , $this->TimestampCreated , $this->TimestampModified , $this->Version , $this->Description , $this->MappingName , $this->TimeStampExported , $this->CreatedByAgentID , $this->ModifiedByAgentID );
           } 
           $statement->execute();
           if ($statement->num_rows()!=1) {
               $this->error = $statement->error; 
           }
           $statement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }
        if ($this->error=='') { 
            $returnvalue = true;
        };

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function delete() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        if($this->SpExportSchemaMappingID != NULL) {
           $sql = 'SELECT SpExportSchemaMappingID, TimestampCreated, TimestampModified, Version, Description, MappingName, TimeStampExported, CreatedByAgentID, ModifiedByAgentID FROM spexportschemamapping WHERE SpExportSchemaMappingID = "'.$this->SpExportSchemaMappingID.'"  ' ;

           $preparedsql = 'SELECT  FROM spexportschemamapping WHERE  and SpExportSchemaMappingID = ?  ' ;
        if ($statement = $connection->prepare($preparedsql)) { 
           $statement->bind_param("ississsii", $this->SpExportSchemaMappingID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->Description, $this->MappingName, $this->TimeStampExported, $this->CreatedByAgentID, $this->ModifiedByAgentID);
           $statement->execute();
           $statement->store_result();
           if ($statement->num_rows()==1) {
                $sql = 'DELETE FROM spexportschemamapping WHERE  and SpExportSchemaMappingID = ?  ';
                if ($stmt_delete = $connection->prepare($sql)) { 
                   $stmt_delete->bind_param("ississsii", $this->SpExportSchemaMappingID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->Description, $this->MappingName, $this->TimeStampExported, $this->CreatedByAgentID, $this->ModifiedByAgentID);
                   if ($stmnt_delete->execute()) { 
                       $returnvalue = true;
                   } else {
                       $this->error = mysqli_error($connection); 
                   }
                   $stmt_delete->close();
                }
           } else { 
               $this->error = mysqli_error($connection); 
           }
           $tatement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }

            $this->loaded = true;
            // record was deleted, so set PK to null
            $this->SpExportSchemaMappingID = NULL; 
        } else { 
           throw new Exception('Unable to identify which record to delete, primary key is not set');
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function count() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        $sql = 'SELECT count(*)  FROM spexportschemamapping';
        if ($result = $connection->query($sql)) { 
           if ($result->num_rows()==1) {
             $row = $result->fetch_row();
             if ($row) {
                $returnvalue = $row[0];
             }
           }
        } else { 
           $this->error = mysqli_error($connection); 
        }
        mysqli_free_result($result);

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function loadArrayKeyValueSearch($searchTermArray) {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = array();
       $and = '';
       $wherebit = 'WHERE ';
       foreach($searchTermArray as $fieldname => $searchTerm) {
           if ($this->hasField($fieldname)) { 
               $operator = '='; 
               // change to a like search if a wildcard character is present
               if (!(strpos($searchTerm,'%')===false)) { $operator = 'like'; }
               if (!(strpos($searchTerm,'_')===false)) { $operator = 'like'; }
               if ($searchTerm=='[NULL]') { 
                   $wherebit .= "$and ($fieldname is null or $fieldname='') "; 
               } else { 
                   $wherebit .= "$and $fieldname $operator ? ";
                   $types = $types . $this->MySQLiFieldType($fieldname);
               } 
               $and = ' and ';
           }
       }
       $sql = "SELECT SpExportSchemaMappingID FROM spexportschemamapping $wherebit";
       if ($wherebit=='') { 
             $this->error = 'Error: No search terms provided';
       } else {
          $statement = $connection->prepare($sql);
          $vars = Array();
          $vars[] = $types;
          $i = 0;
          foreach ($searchTermArray as $value) { 
               $varname = 'bind'.$i;  // create a variable name
               $$varname = $value;    // using that variable name store the value 
               $vars[] = &$$varname;  // add a reference to the variable to the array
               $i++;
           }
           //$vars[] contains $types followed by references to variables holding each value in $searchTermArray.
          call_user_func_array(array($statement,'bind_param'),$vars);
          //$statement->bind_param($types,$names);
          $statement->execute();
          $statement->bind_result($id);
          $ids = array();
          while ($statement->fetch()) {
              $ids[] = $id;
          } // double loop to allow all data to be retrieved before preparing a new statement. 
          $statement->close();
          for ($i=0;$i<count($ids);$i++) {
              $obj = new huh_spexportschemamapping();
              $obj->load($ids[$i]);
              $returnvalue[] = $obj;
              $result=true;
          }
          if ($result===false) { $this->error = mysqli_error($connection); }
       }
       return $returnvalue;
   }	

   //---------------------------------------------------------------------------

// TODO: *************** link to related tables 
  public function loadLinkedFromsp_schema_mapping() { 
      // ForeignKey in: sp_schema_mapping
      $t = new sp_schema_mapping();
  } 
  public function loadLinkedFromspexportschemaitemmapping() { 
      // ForeignKey in: spexportschemaitemmapping
      $t = new spexportschemaitemmapping();
  } 

   //---------------------------------------------------------------------------

  public function loadLinkedTo() { 
     $returnvalue = array(); 
       // fk: CreatedByAgentID
      $t = new agent();
      $t->load(getCreatedByAgentID());
      $returnvalue[CreatedByAgentID] = $t;
       // fk: ModifiedByAgentID
      $t = new agent();
      $t->load(getModifiedByAgentID());
      $returnvalue[ModifiedByAgentID] = $t;
     return $returnvalue;
  } 
   // Returns an array of primary key values (id) and concatenated values of all other fields (fields)
   // wrapped as $values in: '{ "identifier":"id", "items": [ '.$values.' ] }';
   // when druid_handler.php is called with druid_action=returnfkjson
   // Used with dojo dijit.form.FilteringSelect to submit surrogate numeric key values from a text picklist 
   public function keySelectAllConcatJSON($orderby='ASC') {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
       $returnvalue = '';
       $order = '';
       if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
       $sql = "SELECT SpExportSchemaMappingID, concat(IFNULL(TimestampCreated,'') || ' ' || IFNULL(TimestampModified,'') || ' ' || IFNULL(Version,'') || ' ' || IFNULL(Description,'') || ' ' || IFNULL(MappingName,'') || ' ' || IFNULL(TimeStampExported,'') || ' ' || IFNULL(CreatedByAgentID,'') || ' ' || IFNULL(ModifiedByAgentID,'')) FROM spexportschemamapping order by CreatedByAgentID, ModifiedByAgentID $order ";
       $comma = '';
       if ($result = $connection->query($sql)) { 
          while ($row = $result->fetch_row()) {
             if ($row) {
                $pkval = trim($row[0]);
                $fval = trim($row[1]);
                if ($pkval!='') { 
                    $pkval = str_replace('"','&quot;',$pkval);
                    $fval = str_replace('"','&quot;',$fval);
                    $returnvalue .= $comma . ' { "id":"'.$pkval.'", "fields": "'.$fval.'" } ';
                    $comma = ', ';
                }
             }
          }
       } else { 
          $this->error = mysqli_error(); 
       }
       $result->close();
       return $returnvalue;
   }
   //---------------------------------------------------------------------------

   public function keySelectDistinctJSON($field,$orderby='ASC') {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = '';
       if ($this->hasField($field)) { 
          $order = '';
          $fieldesc = mysql_escape_string($field);
          if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
          $preparemysql = "SELECT DISTINCT $fieldesc FROM spexportschemamapping order by $fieldesc $order ";
          $comma = '';
         if ($stmt = $connection->prepare($preparemysql)) { 
             $stmt->execute();
             $stmt->bind_result($val);
             while ($stmt->fetch()) {
                $val = trim($val);                if ($val!='') { 
                    $val = str_replace('"','&quot;',$val);
                    $returnvalue .= $comma . ' { "'.$field.'":"'.$val.'" } ';
                    $comma = ', ';
                 }
             }
             $stmt->close();
         }
       }
       return $returnvalue;
   }
   //---------------------------------------------------------------------------


   //---------------------------------------------------------------------------

   // Each field with an index has a load array method generated for it.
   public function loadArrayByCreatedByAgentID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT SpExportSchemaMappingID FROM spexportschemamapping WHERE CreatedByAgentID $operator '$searchTerm'";
        $preparedsql = "SELECT SpExportSchemaMappingID FROM spexportschemamapping WHERE CreatedByAgentID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_spexportschemamapping();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }
   public function loadArrayByModifiedByAgentID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT SpExportSchemaMappingID FROM spexportschemamapping WHERE ModifiedByAgentID $operator '$searchTerm'";
        $preparedsql = "SELECT SpExportSchemaMappingID FROM spexportschemamapping WHERE ModifiedByAgentID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_spexportschemamapping();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }

   //---------------------------------------------------------------------------

   // Each fulltext index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each field with an index has a select distinct method generated for it.
   public function selectDistinctCreatedByAgentID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' CreatedByAgentID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, CreatedByAgentID FROM spexportschemamapping group by CreatedByAgentID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&CreatedByAgentID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }
   public function selectDistinctModifiedByAgentID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' ModifiedByAgentID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, ModifiedByAgentID FROM spexportschemamapping group by ModifiedByAgentID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&ModifiedByAgentID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }

   public function keySelectDistinct($fieldname,$startline,$link,$endline,$includecount=false,$orderbycount=false) {
       $returnvalue = '';
       switch ($fieldname) { 
          case 'CreatedByAgentID':
             $returnvalue = $this->selectDistinctCreatedByAgentID($startline,$link,$endline,$includecount,$orderbycount);
             break;
          case 'ModifiedByAgentID':
             $returnvalue = $this->selectDistinctModifiedByAgentID($startline,$link,$endline,$includecount,$orderbycount);
             break;
       }
       return $returnvalue;
    }

   //---------------------------------------------------------------------------

   public function hasField($fieldname) {
       $returnvalue = false;
       if (trim($fieldname)!='' && trim($fieldname)!=',') {
            if (strpos(self::FIELDLIST," $fieldname, ")!==false) { 
               $returnvalue = true;
            }
       }
       return $returnvalue;
    }
   //---------------------------------------------------------------------------

}


// Write your own views by extending this class.
// Place your extended views in a separate file and you
// can use Druid to regenerate the spexportschemamapping.php file to reflect changes
// in the underlying database without overwriting your custom views of the data.
// 
class huh_spexportschemamappingView implements viewer
{
   var $model = null;
   public function setModel($aModel) { 
       $this->model = $aModel;
   }
   // @param $includeRelated default true shows rows from other tables through foreign key relationships.
   // @param $editLinkURL default '' allows adding a link to show this record in an editing form.
   public function getDetailsView($includeRelated=true, $editLinkURL='') {
       $returnvalue = '<ul>';
       $editLinkURL=trim($editLinkURL);
       $model = $this->model;
       $primarykeys = $model->schemaPK();
       if ($editLinkURL!='') { 
          if (!preg_match('/\&$/',$editLinkURL)) { $editLinkURL .= '&'; } 
          $nullpk = false; 
          foreach ($primarykeys as $primarykey) { 
              // Add fieldname=value pairs for primary key(s) to editLinkURL. 
              $editLinkURL .= urlencode($primarykey) . '=' . urlencode($model->keyGet($primarykey));
              if ($model->keyGet($primarykey)=='') { $nullpk = true; } 
          }
          if (!$nullpk) { $returnvalue .= "<li>huh_spexportschemamapping <a href='$editLinkURL'>Edit</a></li>\n";  } 
       }
       $returnvalue .= "<li>".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID.": ".$model->getSpExportSchemaMappingID()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::TIMESTAMPCREATED.": ".$model->getTimestampCreated()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::TIMESTAMPMODIFIED.": ".$model->getTimestampModified()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::VERSION.": ".$model->getVersion()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::DESCRIPTION.": ".$model->getDescription()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::MAPPINGNAME.": ".$model->getMappingName()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::TIMESTAMPEXPORTED.": ".$model->getTimeStampExported()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::CREATEDBYAGENTID.": ".$model->getCreatedByAgentID()."</li>\n";
       $returnvalue .= "<li>".huh_spexportschemamapping::MODIFIEDBYAGENTID.": ".$model->getModifiedByAgentID()."</li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
           $returnvalue .= "<li>agent</li>";
           $t_agent = new huh_agent();
           $t_agentView = new huh_agentView();
           $t_agentView->setModel($t_agent);
           if ($model->getCreatedByAgentID() != '') { 
               $t_agent->load($model->getCreatedByAgentID());
               $returnvalue .= $t_agentView->getDetailsView(false);
           }
           $returnvalue .= "<li>agent</li>";
           $t_agent = new huh_agent();
           $t_agentView = new huh_agentView();
           $t_agentView->setModel($t_agent);
           if ($model->getModifiedByAgentID() != '') { 
               $t_agent->load($model->getModifiedByAgentID());
               $returnvalue .= $t_agentView->getDetailsView(false);
           }

        }
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getJSON() {
       $returnvalue = '{ ';
       $model = $this->model;
       $returnvalue .= '"'.huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID.': "'.$model->getSpExportSchemaMappingID().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::TIMESTAMPCREATED.': "'.$model->getTimestampCreated().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::TIMESTAMPMODIFIED.': "'.$model->getTimestampModified().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::VERSION.': "'.$model->getVersion().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::DESCRIPTION.': "'.$model->getDescription().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::MAPPINGNAME.': "'.$model->getMappingName().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::TIMESTAMPEXPORTED.': "'.$model->getTimeStampExported().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::CREATEDBYAGENTID.': "'.$model->getCreatedByAgentID().'",';
       $returnvalue .= '"'.huh_spexportschemamapping::MODIFIEDBYAGENTID.': "'.$model->getModifiedByAgentID().'" }';
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getTableRowView() {
       $returnvalue = '<tr>';
       $model = $this->model;
       $returnvalue .= "<td>".$model->getSpExportSchemaMappingID()."</td>\n";
       $returnvalue .= "<td>".$model->getTimestampCreated()."</td>\n";
       $returnvalue .= "<td>".$model->getTimestampModified()."</td>\n";
       $returnvalue .= "<td>".$model->getVersion()."</td>\n";
       $returnvalue .= "<td>".$model->getDescription()."</td>\n";
       $returnvalue .= "<td>".$model->getMappingName()."</td>\n";
       $returnvalue .= "<td>".$model->getTimeStampExported()."</td>\n";
       $returnvalue .= "<td>".$model->getCreatedByAgentID()."</td>\n";
       $returnvalue .= "<td>".$model->getModifiedByAgentID()."</td>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getHeaderRow() {
       $returnvalue = '<tr>';
       $returnvalue .= "<th>".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::TIMESTAMPCREATED."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::TIMESTAMPMODIFIED."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::VERSION."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::DESCRIPTION."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::MAPPINGNAME."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::TIMESTAMPEXPORTED."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::CREATEDBYAGENTID."</th>\n";
       $returnvalue .= "<th>".huh_spexportschemamapping::MODIFIEDBYAGENTID."</th>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getEditFormDojoView($includeRelated=true) {
       $model = $this->model;
       if ($model->PK()=='') { $addform=true; } else { $addform=false; } 
       $returnvalue = '';
       $id = trim($model->PK());
       $feedback = "status$id";
       $formname = "editform$id";
       $script = '
  var saveprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function save'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Saving...";
     dojo.xhrGet(saveprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_spexportschemamapping">';
       $returnvalue .= "<div id='div_$formnane' >";
       if($addform) { 
          $returnvalue .= "Add a new SpExportSchemaMappingID"; 
       } else { 
         $returnvalue .= "<div><label for=".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID.">SPEXPORTSCHEMAMAPPINGID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID." id=".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID." value='".$model->getSpExportSchemaMappingID()."'  style=' width:".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID_SIZE ."' ></div>\n";
       }  
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::TIMESTAMPCREATED.">TIMESTAMPCREATED</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spexportschemamapping::TIMESTAMPCREATED." id=".huh_spexportschemamapping::TIMESTAMPCREATED." value='".$model->getTimestampCreated()."'  style=' width:".huh_spexportschemamapping::TIMESTAMPCREATED_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::TIMESTAMPCREATED_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::TIMESTAMPMODIFIED.">TIMESTAMPMODIFIED</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spexportschemamapping::TIMESTAMPMODIFIED." id=".huh_spexportschemamapping::TIMESTAMPMODIFIED." value='".$model->getTimestampModified()."'  style=' width:".huh_spexportschemamapping::TIMESTAMPMODIFIED_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::TIMESTAMPMODIFIED_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::VERSION.">VERSION</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spexportschemamapping::VERSION." id=".huh_spexportschemamapping::VERSION." value='".$model->getVersion()."'  style=' width:".huh_spexportschemamapping::VERSION_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::VERSION_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::DESCRIPTION.">DESCRIPTION</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_spexportschemamapping::DESCRIPTION." id=".huh_spexportschemamapping::DESCRIPTION." >".$model->getDescription()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::MAPPINGNAME.">MAPPINGNAME</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spexportschemamapping::MAPPINGNAME." id=".huh_spexportschemamapping::MAPPINGNAME." value='".$model->getMappingName()."'  style=' width:".huh_spexportschemamapping::MAPPINGNAME_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::MAPPINGNAME_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::TIMESTAMPEXPORTED.">TIMESTAMPEXPORTED</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spexportschemamapping::TIMESTAMPEXPORTED." id=".huh_spexportschemamapping::TIMESTAMPEXPORTED." value='".$model->getTimeStampExported()."'  style=' width:".huh_spexportschemamapping::TIMESTAMPEXPORTED_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::TIMESTAMPEXPORTED_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::CREATEDBYAGENTID.">CREATEDBYAGENTID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spexportschemamapping::CREATEDBYAGENTID." id=".huh_spexportschemamapping::CREATEDBYAGENTID." value='".$model->getCreatedByAgentID()."'  style=' width:".huh_spexportschemamapping::CREATEDBYAGENTID_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::CREATEDBYAGENTID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spexportschemamapping::MODIFIEDBYAGENTID.">MODIFIEDBYAGENTID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spexportschemamapping::MODIFIEDBYAGENTID." id=".huh_spexportschemamapping::MODIFIEDBYAGENTID." value='".$model->getModifiedByAgentID()."'  style=' width:".huh_spexportschemamapping::MODIFIEDBYAGENTID_SIZE ."em;  '  maxlength='".huh_spexportschemamapping::MODIFIEDBYAGENTID_SIZE ."' ></div>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= "<li><input name=save id=save type=button value='Save' onclick='$script  save$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</div>';
       $returnvalue .= '</form>';
       if(!$addform) { 
          // show delete button if editing an existing record 
       $id = trim($model->PK());
       $feedback = "deletestatus$id";
       $formname = "deleteform$id";
       $script = '
  var deleteprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function deleterecord'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Deleting...";
     dojo.xhrGet(deleteprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=delete>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_table value="huh_spexportschemamapping">';
       $returnvalue .= '<input type=hidden name=SpExportSchemaMappingID id=druid_table value="'.$id.'">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li><input name=delete id=save type=button value='Delete' onclick='$script  deleterecord$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       } 
       return  $returnvalue;
   }
   public function getEditFormView($includeRelated=true) {
       $model = $this->model;
       $returnvalue = '<form method=get action=druid_handler.php>';
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_spexportschemamapping">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li>SPEXPORTSCHEMAMAPPINGID<input type=text name=".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID." id=".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID." value='".$model->getSpExportSchemaMappingID()."'  size='".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID_SIZE ."'  maxlength='".huh_spexportschemamapping::SPEXPORTSCHEMAMAPPINGID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TIMESTAMPCREATED<input type=text name=".huh_spexportschemamapping::TIMESTAMPCREATED." id=".huh_spexportschemamapping::TIMESTAMPCREATED." value='".$model->getTimestampCreated()."'  size='".huh_spexportschemamapping::TIMESTAMPCREATED_SIZE ."'  maxlength='".huh_spexportschemamapping::TIMESTAMPCREATED_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TIMESTAMPMODIFIED<input type=text name=".huh_spexportschemamapping::TIMESTAMPMODIFIED." id=".huh_spexportschemamapping::TIMESTAMPMODIFIED." value='".$model->getTimestampModified()."'  size='".huh_spexportschemamapping::TIMESTAMPMODIFIED_SIZE ."'  maxlength='".huh_spexportschemamapping::TIMESTAMPMODIFIED_SIZE ."' ></li>\n";
       $returnvalue .= "<li>VERSION<input type=text name=".huh_spexportschemamapping::VERSION." id=".huh_spexportschemamapping::VERSION." value='".$model->getVersion()."'  size='".huh_spexportschemamapping::VERSION_SIZE ."'  maxlength='".huh_spexportschemamapping::VERSION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>DESCRIPTION<input type=text name=".huh_spexportschemamapping::DESCRIPTION." id=".huh_spexportschemamapping::DESCRIPTION." value='".$model->getDescription()."'  size='51'  maxlength='".huh_spexportschemamapping::DESCRIPTION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>MAPPINGNAME<input type=text name=".huh_spexportschemamapping::MAPPINGNAME." id=".huh_spexportschemamapping::MAPPINGNAME." value='".$model->getMappingName()."'  size='".huh_spexportschemamapping::MAPPINGNAME_SIZE ."'  maxlength='".huh_spexportschemamapping::MAPPINGNAME_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TIMESTAMPEXPORTED<input type=text name=".huh_spexportschemamapping::TIMESTAMPEXPORTED." id=".huh_spexportschemamapping::TIMESTAMPEXPORTED." value='".$model->getTimeStampExported()."'  size='".huh_spexportschemamapping::TIMESTAMPEXPORTED_SIZE ."'  maxlength='".huh_spexportschemamapping::TIMESTAMPEXPORTED_SIZE ."' ></li>\n";
       $returnvalue .= "<li>CREATEDBYAGENTID<input type=text name=".huh_spexportschemamapping::CREATEDBYAGENTID." id=".huh_spexportschemamapping::CREATEDBYAGENTID." value='".$model->getCreatedByAgentID()."'  size='".huh_spexportschemamapping::CREATEDBYAGENTID_SIZE ."'  maxlength='".huh_spexportschemamapping::CREATEDBYAGENTID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>MODIFIEDBYAGENTID<input type=text name=".huh_spexportschemamapping::MODIFIEDBYAGENTID." id=".huh_spexportschemamapping::MODIFIEDBYAGENTID." value='".$model->getModifiedByAgentID()."'  size='".huh_spexportschemamapping::MODIFIEDBYAGENTID_SIZE ."'  maxlength='".huh_spexportschemamapping::MODIFIEDBYAGENTID_SIZE ."' ></li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= '<li><input type=submit value="Save"></li>';
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       return  $returnvalue;
   }
}

//==============================================================================
?>
