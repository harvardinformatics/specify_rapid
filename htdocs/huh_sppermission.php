<?php
//==============================================================================
//===   sppermission.php                         
//===   Autogenerated by Druid from MySQL db Build:6
//==============================================================================

include_once("druid_interfaces.php");

class huh_sppermission implements  model , loadableModel, saveableModel, tableSchema 
{
   // These constants hold the sizes the fields in this table in the database.
   const SPPERMISSIONID_SIZE  = 11; //INTEGER
   const ACTIONS_SIZE         = 255; //LONGVARCHAR
   const NAME_SIZE            = 64; //64
   const PERMISSIONCLASS_SIZE = 255; //LONGVARCHAR
   const TARGETID_SIZE        = 11; //INTEGER
    // These constants hold the field names of the table in the database. 
   const SPPERMISSIONID    = 'SpPermissionID';
   const ACTIONS           = 'Actions';
   const NAME              = 'Name';
   const PERMISSIONCLASS   = 'PermissionClass';
   const TARGETID          = 'TargetId';

   //---------------------------------------------------------------------------

   // interface tableSchema implementation
   // schemaPK returns array of primary key field names
   public function schemaPK() {
       return $this->primaryKeyArray;
   } 
   // schemaHaveDistinct returns array of field names for which selectDistinct{fieldname} methods are available.
   public function schemaHaveDistinct() {
       return $this->selectDistinctFieldsArray;
   } 
   // schemaFields returns array of all field names
   public function schemaFields() { 
       return $this->allFieldsArray;
   } 
/*  Example sanitized retrieval of variable matching object variables from $_GET 
/*  Customize these to limit each variable to narrowest possible set of known good values. 

  $SpPermissionID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['SpPermissionID']), 0, 11);
  $Actions = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['Actions']), 0, 255);
  $Name = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['Name']), 0, 64);
  $PermissionClass = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['PermissionClass']), 0, 255);
  $TargetId = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['TargetId']), 0, 11);
*/

   //---------------------------------------------------------------------------

   private $SpPermissionID; // PK INTEGER 
   private $Actions; // LONGVARCHAR 
   private $Name; // VARCHAR(64) 
   private $PermissionClass; // LONGVARCHAR 
   private $TargetId; // INTEGER 
   private $dirty;
   private $loaded;
   private $error;
   const FIELDLIST = ' SpPermissionID, Actions, Name, PermissionClass, TargetId, ';
   const PKFIELDLIST = ' SpPermissionID, ';
   const NUMBER_OF_PRIMARY_KEYS = 1;
   private $primaryKeyArray = array( 1 => 'SpPermissionID'  ) ;
   private $allFieldsArray = array( 0 => 'SpPermissionID' , 1 => 'Actions' , 2 => 'Name' , 3 => 'PermissionClass' , 4 => 'TargetId'  ) ;
   private $selectDistinctFieldsArray = array(  ) ;

   //---------------------------------------------------------------------------

   // constructor 
   function huh_sppermission(){
       $this->SpPermissionID = NULL;
       $this->Actions = '';
       $this->Name = '';
       $this->PermissionClass = '';
       $this->TargetId = '';
       $this->dirty = false;
       $this->loaded = false;
       $this->error = '';
   }

   private function l_addslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = addslashes($value);
      }
      return $retval;
   }
   private function l_stripslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = stripslashes($value);
      }
      return $retval;
   }
   public function isDirty() {
       return $this->dirty;
   }
   public function isLoaded() {
       return $this->loaded;
   }
   public function errorMessage() {
       return $this->error;
   }

   //---------------------------------------------------------------------------

   public function keyValueSet($fieldname,$value) {
       $returnvalue = false;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpPermissionID') { $returnvalue = $this->setSpPermissionID($value); } 
             if ($fieldname=='Actions') { $returnvalue = $this->setActions($value); } 
             if ($fieldname=='Name') { $returnvalue = $this->setName($value); } 
             if ($fieldname=='PermissionClass') { $returnvalue = $this->setPermissionClass($value); } 
             if ($fieldname=='TargetId') { $returnvalue = $this->setTargetId($value); } 
             $returnvalue = true;
          }
          catch (exception $e) { ;
              $returnvalue = false;
              throw new Exception('Field Set Error'.$e->getMessage()); 
          }
       } else { 
          throw new Exception('No Such field'); 
       }  
       return $returnvalue;
   }
   public function keyGet($fieldname) {
       $returnvalue = null;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpPermissionID') { $returnvalue = $this->getSpPermissionID(); } 
             if ($fieldname=='Actions') { $returnvalue = $this->getActions(); } 
             if ($fieldname=='Name') { $returnvalue = $this->getName(); } 
             if ($fieldname=='PermissionClass') { $returnvalue = $this->getPermissionClass(); } 
             if ($fieldname=='TargetId') { $returnvalue = $this->getTargetId(); } 
          }
          catch (exception $e) { ;
              $returnvalue = null;
          }
       }
       return $returnvalue;
   }
/*SpPermissionID*/
   public function getSpPermissionID() {
       if ($this->SpPermissionID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SpPermissionID));
       }
   }
   public function setSpPermissionID($SpPermissionID) {
       if (strlen(preg_replace('/[^0-9]/','',$SpPermissionID)) > huh_sppermission::SPPERMISSIONID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $SpPermissionID = trim($SpPermissionID);
       if (!ctype_digit(strval($SpPermissionID)) && trim(strval($SpPermissionID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->SpPermissionID = $this->l_addslashes($SpPermissionID);
       $this->dirty = true;
   }
/*Actions*/
   public function getActions() {
       if ($this->Actions==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->Actions));
       }
   }
   public function setActions($Actions) {
       if (strlen($Actions) > huh_sppermission::ACTIONS_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->Actions = $this->l_addslashes($Actions);
       $this->dirty = true;
   }
/*Name*/
   public function getName() {
       if ($this->Name==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->Name));
       }
   }
   public function setName($Name) {
       if (strlen($Name) > huh_sppermission::NAME_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->Name = $this->l_addslashes($Name);
       $this->dirty = true;
   }
/*PermissionClass*/
   public function getPermissionClass() {
       if ($this->PermissionClass==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->PermissionClass));
       }
   }
   public function setPermissionClass($PermissionClass) {
       if (strlen($PermissionClass) > huh_sppermission::PERMISSIONCLASS_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->PermissionClass = $this->l_addslashes($PermissionClass);
       $this->dirty = true;
   }
/*TargetId*/
   public function getTargetId() {
       if ($this->TargetId==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TargetId));
       }
   }
   public function setTargetId($TargetId) {
       if (strlen(preg_replace('/[^0-9]/','',$TargetId)) > huh_sppermission::TARGETID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $TargetId = trim($TargetId);
       if (!ctype_digit(strval($TargetId)) && trim(strval($TargetId))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->TargetId = $this->l_addslashes($TargetId);
       $this->dirty = true;
   }
   public function PK() { // get value of primary key 
        $returnvalue = '';
        $returnvalue .= $this->getSpPermissionID();
        return $returnvalue;
   }
   public function PKArray() { // get name and value of primary key fields 
        $returnvalue = array();
        $returnvalue['SpPermissionID'] = $this->getSpPermissionID();
        return $returnvalue;
   }
   public function NumberOfPrimaryKeyFields() { // returns the number of primary key fields defined for this table 
        return 1;
   }

   // Constants holding the mysqli field type character (s,i,d) for each field
  const C_SpPermissionIDMYSQLI_TYPE = 'i';
  const C_ActionsMYSQLI_TYPE = 's';
  const C_NameMYSQLI_TYPE = 's';
  const C_PermissionClassMYSQLI_TYPE = 's';
  const C_TargetIdMYSQLI_TYPE = 'i';

   // function to obtain the mysqli field type character from a fieldname
   public function MySQLiFieldType($aFieldname) { 
      $retval = '';
      if ($aFieldname=='SpPermissionID') { $retval = self::C_SpPermissionIDMYSQLI_TYPE; }
      if ($aFieldname=='Actions') { $retval = self::C_ActionsMYSQLI_TYPE; }
      if ($aFieldname=='Name') { $retval = self::C_NameMYSQLI_TYPE; }
      if ($aFieldname=='PermissionClass') { $retval = self::C_PermissionClassMYSQLI_TYPE; }
      if ($aFieldname=='TargetId') { $retval = self::C_TargetIdMYSQLI_TYPE; }
      return $retval;
   }

   // Function load() can take either the value of the primary key which uniquely identifies a particular row
   // or an array of array('primarykeyfieldname'=>'value') in the case of a single field primary key
   // or an array of fieldname value pairs in the case of multiple field primary key.
   public function load($pk) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        try {
             if (is_array($pk)) { 
                 $this->setSpPermissionID($pk[SpPermissionID]);
             } else { ;
                 $this->setSpPermissionID($pk);
             };
        } 
        catch (Exception $e) { 
             throw new Exception($e->getMessage());
        }
        if($this->SpPermissionID != NULL) {
           $sql = 'SELECT SpPermissionID, Actions, Name, PermissionClass, TargetId FROM sppermission WHERE SpPermissionID = '.$this->SpPermissionID ;

           $preparesql = 'SELECT SpPermissionID, Actions, Name, PermissionClass, TargetId FROM sppermission WHERE SpPermissionID = ? ';

           if ($statement = $connection->prepare($preparesql)) { 
              $statement->bind_param("i", $this->SpPermissionID);
              $statement->execute();
              $statement->bind_result($this->SpPermissionID, $this->Actions, $this->Name, $this->PermissionClass, $this->TargetId);
              $statement->fetch();
              $statement->close();
           }

            $this->loaded = true;
            $this->dirty = false;
        } else { 
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   // Function save() will either save the current record or insert a new record.
   // Inserts new record if the primary key field in this table is null 
   // for this instance of this object.
   // Otherwise updates the record identified by the primary key value.
   public function save() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        // Test to see if this is an insert or update.
        if ($this->SpPermissionID!= NULL) {
            $sql  = 'UPDATE  sppermission SET ';
            $isInsert = false;
            $sql .=  "Actions = ? ";
            $sql .=  ", Name = ? ";
            $sql .=  ", PermissionClass = ? ";
            $sql .=  ", TargetId = ? ";

            $sql .= "  WHERE SpPermissionID = ? ";
        } else {
            $sql  = 'INSERT INTO sppermission ';
            $isInsert = true;
if ($this->PK==NULL) { throw new Exception('Can\'t insert record with null primary key for this table'); }
            $sql .= '( SpPermissionID ,  Actions ,  Name ,  PermissionClass ,  TargetId ) VALUES (';
            $sql .=  "  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .= ')';

        }
        if ($statement = $connection->prepare($sql)) { 
           if ($this->SpPermissionID!= NULL ) {
              $statement->bind_param("isssii", $this->SpPermissionID , $this->Actions , $this->Name , $this->PermissionClass , $this->TargetId , $this->SpPermissionID );
           } else { 
              $statement->bind_param("isssi", $this->SpPermissionID , $this->Actions , $this->Name , $this->PermissionClass , $this->TargetId );
           } 
           $statement->execute();
           if ($statement->num_rows()!=1) {
               $this->error = $statement->error; 
           }
           $statement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }
        if ($this->error=='') { 
            $returnvalue = true;
        };

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function delete() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        if($this->SpPermissionID != NULL) {
           $sql = 'SELECT SpPermissionID, Actions, Name, PermissionClass, TargetId FROM sppermission WHERE SpPermissionID = "'.$this->SpPermissionID.'"  ' ;

           $preparedsql = 'SELECT  FROM sppermission WHERE  and SpPermissionID = ?  ' ;
        if ($statement = $connection->prepare($preparedsql)) { 
           $statement->bind_param("isssi", $this->SpPermissionID, $this->Actions, $this->Name, $this->PermissionClass, $this->TargetId);
           $statement->execute();
           $statement->store_result();
           if ($statement->num_rows()==1) {
                $sql = 'DELETE FROM sppermission WHERE  and SpPermissionID = ?  ';
                if ($stmt_delete = $connection->prepare($sql)) { 
                   $stmt_delete->bind_param("isssi", $this->SpPermissionID, $this->Actions, $this->Name, $this->PermissionClass, $this->TargetId);
                   if ($stmnt_delete->execute()) { 
                       $returnvalue = true;
                   } else {
                       $this->error = mysqli_error($connection); 
                   }
                   $stmt_delete->close();
                }
           } else { 
               $this->error = mysqli_error($connection); 
           }
           $tatement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }

            $this->loaded = true;
            // record was deleted, so set PK to null
            $this->SpPermissionID = NULL; 
        } else { 
           throw new Exception('Unable to identify which record to delete, primary key is not set');
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function count() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        $sql = 'SELECT count(*)  FROM sppermission';
        if ($result = $connection->query($sql)) { 
           if ($result->num_rows()==1) {
             $row = $result->fetch_row();
             if ($row) {
                $returnvalue = $row[0];
             }
           }
        } else { 
           $this->error = mysqli_error($connection); 
        }
        mysqli_free_result($result);

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function loadArrayKeyValueSearch($searchTermArray) {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = array();
       $and = '';
       $wherebit = 'WHERE ';
       foreach($searchTermArray as $fieldname => $searchTerm) {
           if ($this->hasField($fieldname)) { 
               $operator = '='; 
               // change to a like search if a wildcard character is present
               if (!(strpos($searchTerm,'%')===false)) { $operator = 'like'; }
               if (!(strpos($searchTerm,'_')===false)) { $operator = 'like'; }
               if ($searchTerm=='[NULL]') { 
                   $wherebit .= "$and ($fieldname is null or $fieldname='') "; 
               } else { 
                   $wherebit .= "$and $fieldname $operator ? ";
                   $types = $types . $this->MySQLiFieldType($fieldname);
               } 
               $and = ' and ';
           }
       }
       $sql = "SELECT SpPermissionID FROM sppermission $wherebit";
       if ($wherebit=='') { 
             $this->error = 'Error: No search terms provided';
       } else {
          $statement = $connection->prepare($sql);
          $vars = Array();
          $vars[] = $types;
          $i = 0;
          foreach ($searchTermArray as $value) { 
               $varname = 'bind'.$i;  // create a variable name
               $$varname = $value;    // using that variable name store the value 
               $vars[] = &$$varname;  // add a reference to the variable to the array
               $i++;
           }
           //$vars[] contains $types followed by references to variables holding each value in $searchTermArray.
          call_user_func_array(array($statement,'bind_param'),$vars);
          //$statement->bind_param($types,$names);
          $statement->execute();
          $statement->bind_result($id);
          $ids = array();
          while ($statement->fetch()) {
              $ids[] = $id;
          } // double loop to allow all data to be retrieved before preparing a new statement. 
          $statement->close();
          for ($i=0;$i<count($ids);$i++) {
              $obj = new huh_sppermission();
              $obj->load($ids[$i]);
              $returnvalue[] = $obj;
              $result=true;
          }
          if ($result===false) { $this->error = mysqli_error($connection); }
       }
       return $returnvalue;
   }	

   //---------------------------------------------------------------------------

// TODO: *************** link to related tables 
  public function loadLinkedFromspprincipal_sppermission() { 
      // ForeignKey in: spprincipal_sppermission
      $t = new spprincipal_sppermission();
  } 

   //---------------------------------------------------------------------------

   // Returns an array of primary key values (id) and concatenated values of all other fields (fields)
   // wrapped as $values in: '{ "identifier":"id", "items": [ '.$values.' ] }';
   // when druid_handler.php is called with druid_action=returnfkjson
   // Used with dojo dijit.form.FilteringSelect to submit surrogate numeric key values from a text picklist 
   public function keySelectAllConcatJSON($orderby='ASC') {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
       $returnvalue = '';
       $order = '';
       if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
       $sql = "SELECT SpPermissionID, concat(IFNULL(Actions,'') || ' ' || IFNULL(Name,'') || ' ' || IFNULL(PermissionClass,'') || ' ' || IFNULL(TargetId,'')) FROM sppermission order by SpPermissionID $order ";
       $comma = '';
       if ($result = $connection->query($sql)) { 
          while ($row = $result->fetch_row()) {
             if ($row) {
                $pkval = trim($row[0]);
                $fval = trim($row[1]);
                if ($pkval!='') { 
                    $pkval = str_replace('"','&quot;',$pkval);
                    $fval = str_replace('"','&quot;',$fval);
                    $returnvalue .= $comma . ' { "id":"'.$pkval.'", "fields": "'.$fval.'" } ';
                    $comma = ', ';
                }
             }
          }
       } else { 
          $this->error = mysqli_error(); 
       }
       $result->close();
       return $returnvalue;
   }
   //---------------------------------------------------------------------------

   public function keySelectDistinctJSON($field,$orderby='ASC') {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = '';
       if ($this->hasField($field)) { 
          $order = '';
          $fieldesc = mysql_escape_string($field);
          if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
          $preparemysql = "SELECT DISTINCT $fieldesc FROM sppermission order by $fieldesc $order ";
          $comma = '';
         if ($stmt = $connection->prepare($preparemysql)) { 
             $stmt->execute();
             $stmt->bind_result($val);
             while ($stmt->fetch()) {
                $val = trim($val);                if ($val!='') { 
                    $val = str_replace('"','&quot;',$val);
                    $returnvalue .= $comma . ' { "'.$field.'":"'.$val.'" } ';
                    $comma = ', ';
                 }
             }
             $stmt->close();
         }
       }
       return $returnvalue;
   }
   //---------------------------------------------------------------------------


   //---------------------------------------------------------------------------

   // Each field with an index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each fulltext index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each field with an index has a select distinct method generated for it.

   public function keySelectDistinct($fieldname,$startline,$link,$endline,$includecount=false,$orderbycount=false) {
       $returnvalue = '';
       switch ($fieldname) { 
       }
       return $returnvalue;
    }

   //---------------------------------------------------------------------------

   public function hasField($fieldname) {
       $returnvalue = false;
       if (trim($fieldname)!='' && trim($fieldname)!=',') {
            if (strpos(self::FIELDLIST," $fieldname, ")!==false) { 
               $returnvalue = true;
            }
       }
       return $returnvalue;
    }
   //---------------------------------------------------------------------------

}


// Write your own views by extending this class.
// Place your extended views in a separate file and you
// can use Druid to regenerate the sppermission.php file to reflect changes
// in the underlying database without overwriting your custom views of the data.
// 
class huh_sppermissionView implements viewer
{
   var $model = null;
   public function setModel($aModel) { 
       $this->model = $aModel;
   }
   // @param $includeRelated default true shows rows from other tables through foreign key relationships.
   // @param $editLinkURL default '' allows adding a link to show this record in an editing form.
   public function getDetailsView($includeRelated=true, $editLinkURL='') {
       $returnvalue = '<ul>';
       $editLinkURL=trim($editLinkURL);
       $model = $this->model;
       $primarykeys = $model->schemaPK();
       if ($editLinkURL!='') { 
          if (!preg_match('/\&$/',$editLinkURL)) { $editLinkURL .= '&'; } 
          $nullpk = false; 
          foreach ($primarykeys as $primarykey) { 
              // Add fieldname=value pairs for primary key(s) to editLinkURL. 
              $editLinkURL .= urlencode($primarykey) . '=' . urlencode($model->keyGet($primarykey));
              if ($model->keyGet($primarykey)=='') { $nullpk = true; } 
          }
          if (!$nullpk) { $returnvalue .= "<li>huh_sppermission <a href='$editLinkURL'>Edit</a></li>\n";  } 
       }
       $returnvalue .= "<li>".huh_sppermission::SPPERMISSIONID.": ".$model->getSpPermissionID()."</li>\n";
       $returnvalue .= "<li>".huh_sppermission::ACTIONS.": ".$model->getActions()."</li>\n";
       $returnvalue .= "<li>".huh_sppermission::NAME.": ".$model->getName()."</li>\n";
       $returnvalue .= "<li>".huh_sppermission::PERMISSIONCLASS.": ".$model->getPermissionClass()."</li>\n";
       $returnvalue .= "<li>".huh_sppermission::TARGETID.": ".$model->getTargetId()."</li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.

        }
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getJSON() {
       $returnvalue = '{ ';
       $model = $this->model;
       $returnvalue .= '"'.huh_sppermission::SPPERMISSIONID.': "'.$model->getSpPermissionID().'",';
       $returnvalue .= '"'.huh_sppermission::ACTIONS.': "'.$model->getActions().'",';
       $returnvalue .= '"'.huh_sppermission::NAME.': "'.$model->getName().'",';
       $returnvalue .= '"'.huh_sppermission::PERMISSIONCLASS.': "'.$model->getPermissionClass().'",';
       $returnvalue .= '"'.huh_sppermission::TARGETID.': "'.$model->getTargetId().'" }';
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getTableRowView() {
       $returnvalue = '<tr>';
       $model = $this->model;
       $returnvalue .= "<td>".$model->getSpPermissionID()."</td>\n";
       $returnvalue .= "<td>".$model->getActions()."</td>\n";
       $returnvalue .= "<td>".$model->getName()."</td>\n";
       $returnvalue .= "<td>".$model->getPermissionClass()."</td>\n";
       $returnvalue .= "<td>".$model->getTargetId()."</td>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getHeaderRow() {
       $returnvalue = '<tr>';
       $returnvalue .= "<th>".huh_sppermission::SPPERMISSIONID."</th>\n";
       $returnvalue .= "<th>".huh_sppermission::ACTIONS."</th>\n";
       $returnvalue .= "<th>".huh_sppermission::NAME."</th>\n";
       $returnvalue .= "<th>".huh_sppermission::PERMISSIONCLASS."</th>\n";
       $returnvalue .= "<th>".huh_sppermission::TARGETID."</th>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getEditFormDojoView($includeRelated=true) {
       $model = $this->model;
       if ($model->PK()=='') { $addform=true; } else { $addform=false; } 
       $returnvalue = '';
       $id = trim($model->PK());
       $feedback = "status$id";
       $formname = "editform$id";
       $script = '
  var saveprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function save'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Saving...";
     dojo.xhrGet(saveprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_sppermission">';
       $returnvalue .= "<div id='div_$formnane' >";
       if($addform) { 
          $returnvalue .= "Add a new SpPermissionID"; 
       } else { 
         $returnvalue .= "<div><label for=".huh_sppermission::SPPERMISSIONID.">SPPERMISSIONID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_sppermission::SPPERMISSIONID." id=".huh_sppermission::SPPERMISSIONID." value='".$model->getSpPermissionID()."'  style=' width:".huh_sppermission::SPPERMISSIONID_SIZE ."em;  '  maxlength='".huh_sppermission::SPPERMISSIONID_SIZE ."' ></div>\n";
       }  
       $returnvalue .= "<div><label for=".huh_sppermission::ACTIONS.">ACTIONS</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_sppermission::ACTIONS." id=".huh_sppermission::ACTIONS." >".$model->getActions()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_sppermission::NAME.">NAME</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_sppermission::NAME." id=".huh_sppermission::NAME." >".$model->getName()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_sppermission::PERMISSIONCLASS.">PERMISSIONCLASS</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_sppermission::PERMISSIONCLASS." id=".huh_sppermission::PERMISSIONCLASS." >".$model->getPermissionClass()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_sppermission::TARGETID.">TARGETID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_sppermission::TARGETID." id=".huh_sppermission::TARGETID." value='".$model->getTargetId()."'  style=' width:".huh_sppermission::TARGETID_SIZE ."em;  '  maxlength='".huh_sppermission::TARGETID_SIZE ."' ></div>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= "<li><input name=save id=save type=button value='Save' onclick='$script  save$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</div>';
       $returnvalue .= '</form>';
       if(!$addform) { 
          // show delete button if editing an existing record 
       $id = trim($model->PK());
       $feedback = "deletestatus$id";
       $formname = "deleteform$id";
       $script = '
  var deleteprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function deleterecord'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Deleting...";
     dojo.xhrGet(deleteprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=delete>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_table value="huh_sppermission">';
       $returnvalue .= '<input type=hidden name=SpPermissionID id=druid_table value="'.$id.'">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li><input name=delete id=save type=button value='Delete' onclick='$script  deleterecord$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       } 
       return  $returnvalue;
   }
   public function getEditFormView($includeRelated=true) {
       $model = $this->model;
       $returnvalue = '<form method=get action=druid_handler.php>';
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_sppermission">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li>SPPERMISSIONID<input type=text name=".huh_sppermission::SPPERMISSIONID." id=".huh_sppermission::SPPERMISSIONID." value='".$model->getSpPermissionID()."'  size='".huh_sppermission::SPPERMISSIONID_SIZE ."'  maxlength='".huh_sppermission::SPPERMISSIONID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>ACTIONS<input type=text name=".huh_sppermission::ACTIONS." id=".huh_sppermission::ACTIONS." value='".$model->getActions()."'  size='51'  maxlength='".huh_sppermission::ACTIONS_SIZE ."' ></li>\n";
       $returnvalue .= "<li>NAME<input type=text name=".huh_sppermission::NAME." id=".huh_sppermission::NAME." value='".$model->getName()."'  size='51'  maxlength='".huh_sppermission::NAME_SIZE ."' ></li>\n";
       $returnvalue .= "<li>PERMISSIONCLASS<input type=text name=".huh_sppermission::PERMISSIONCLASS." id=".huh_sppermission::PERMISSIONCLASS." value='".$model->getPermissionClass()."'  size='51'  maxlength='".huh_sppermission::PERMISSIONCLASS_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TARGETID<input type=text name=".huh_sppermission::TARGETID." id=".huh_sppermission::TARGETID." value='".$model->getTargetId()."'  size='".huh_sppermission::TARGETID_SIZE ."'  maxlength='".huh_sppermission::TARGETID_SIZE ."' ></li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= '<li><input type=submit value="Save"></li>';
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       return  $returnvalue;
   }
}

//==============================================================================
?>
