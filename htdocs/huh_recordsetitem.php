<?php
//==============================================================================
//===   recordsetitem.php                         
//===   Autogenerated by Druid from MySQL db Build:6
//==============================================================================

include_once("druid_interfaces.php");

class huh_recordsetitem implements  model , loadableModel, saveableModel, tableSchema 
{
   // These constants hold the sizes the fields in this table in the database.
   const RECORDSETITEMID_SIZE = 11; //INTEGER
   const RECORDID_SIZE        = 11; //INTEGER
   const RECORDSETID_SIZE     = 11; //INTEGER
    // These constants hold the field names of the table in the database. 
   const RECORDSETITEMID   = 'RecordSetItemID';
   const RECORDID          = 'RecordId';
   const RECORDSETID       = 'RecordSetID';

   //---------------------------------------------------------------------------

   // interface tableSchema implementation
   // schemaPK returns array of primary key field names
   public function schemaPK() {
       return $this->primaryKeyArray;
   } 
   // schemaHaveDistinct returns array of field names for which selectDistinct{fieldname} methods are available.
   public function schemaHaveDistinct() {
       return $this->selectDistinctFieldsArray;
   } 
   // schemaFields returns array of all field names
   public function schemaFields() { 
       return $this->allFieldsArray;
   } 
/*  Example sanitized retrieval of variable matching object variables from $_GET 
/*  Customize these to limit each variable to narrowest possible set of known good values. 

  $RecordSetItemID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['RecordSetItemID']), 0, 11);
  $RecordId = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['RecordId']), 0, 11);
  $RecordSetID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['RecordSetID']), 0, 11);
*/

   //---------------------------------------------------------------------------

   private $RecordSetItemID; // PK INTEGER 
   private $RecordId; // INTEGER 
   private $RecordSetID; // INTEGER 
   private $dirty;
   private $loaded;
   private $error;
   const FIELDLIST = ' RecordSetItemID, RecordId, RecordSetID, ';
   const PKFIELDLIST = ' RecordSetItemID, ';
   const NUMBER_OF_PRIMARY_KEYS = 1;
   private $primaryKeyArray = array( 1 => 'RecordSetItemID'  ) ;
   private $allFieldsArray = array( 0 => 'RecordSetItemID' , 1 => 'RecordId' , 2 => 'RecordSetID'  ) ;
   private $selectDistinctFieldsArray = array( 2 => 'RecordSetID'  ) ;

   //---------------------------------------------------------------------------

   // constructor 
   function huh_recordsetitem(){
       $this->RecordSetItemID = NULL;
       $this->RecordId = '';
       $this->RecordSetID = '';
       $this->dirty = false;
       $this->loaded = false;
       $this->error = '';
   }

   private function l_addslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = addslashes($value);
      }
      return $retval;
   }
   private function l_stripslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = stripslashes($value);
      }
      return $retval;
   }
   public function isDirty() {
       return $this->dirty;
   }
   public function isLoaded() {
       return $this->loaded;
   }
   public function errorMessage() {
       return $this->error;
   }

   //---------------------------------------------------------------------------

   public function keyValueSet($fieldname,$value) {
       $returnvalue = false;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='RecordSetItemID') { $returnvalue = $this->setRecordSetItemID($value); } 
             if ($fieldname=='RecordId') { $returnvalue = $this->setRecordId($value); } 
             if ($fieldname=='RecordSetID') { $returnvalue = $this->setRecordSetID($value); } 
             $returnvalue = true;
          }
          catch (exception $e) { ;
              $returnvalue = false;
              throw new Exception('Field Set Error'.$e->getMessage()); 
          }
       } else { 
          throw new Exception('No Such field'); 
       }  
       return $returnvalue;
   }
   public function keyGet($fieldname) {
       $returnvalue = null;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='RecordSetItemID') { $returnvalue = $this->getRecordSetItemID(); } 
             if ($fieldname=='RecordId') { $returnvalue = $this->getRecordId(); } 
             if ($fieldname=='RecordSetID') { $returnvalue = $this->getRecordSetID(); } 
          }
          catch (exception $e) { ;
              $returnvalue = null;
          }
       }
       return $returnvalue;
   }
/*RecordSetItemID*/
   public function getRecordSetItemID() {
       if ($this->RecordSetItemID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->RecordSetItemID));
       }
   }
   public function setRecordSetItemID($RecordSetItemID) {
       if (strlen(preg_replace('/[^0-9]/','',$RecordSetItemID)) > huh_recordsetitem::RECORDSETITEMID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $RecordSetItemID = trim($RecordSetItemID);
       if (!ctype_digit(strval($RecordSetItemID)) && trim(strval($RecordSetItemID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->RecordSetItemID = $this->l_addslashes($RecordSetItemID);
       $this->dirty = true;
   }
/*RecordId*/
   public function getRecordId() {
       if ($this->RecordId==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->RecordId));
       }
   }
   public function setRecordId($RecordId) {
       if (strlen(preg_replace('/[^0-9]/','',$RecordId)) > huh_recordsetitem::RECORDID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $RecordId = trim($RecordId);
       if (!ctype_digit(strval($RecordId))) {
             throw new Exception("Value must be an integer");
       }
       $this->RecordId = $this->l_addslashes($RecordId);
       $this->dirty = true;
   }
/*RecordSetID*/
   public function getRecordSetID() {
       if ($this->RecordSetID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->RecordSetID));
       }
   }
   public function setRecordSetID($RecordSetID) {
       if (strlen(preg_replace('/[^0-9]/','',$RecordSetID)) > huh_recordsetitem::RECORDSETID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $RecordSetID = trim($RecordSetID);
       if (!ctype_digit(strval($RecordSetID))) {
             throw new Exception("Value must be an integer");
       }
       $this->RecordSetID = $this->l_addslashes($RecordSetID);
       $this->dirty = true;
   }
   public function PK() { // get value of primary key 
        $returnvalue = '';
        $returnvalue .= $this->getRecordSetItemID();
        return $returnvalue;
   }
   public function PKArray() { // get name and value of primary key fields 
        $returnvalue = array();
        $returnvalue['RecordSetItemID'] = $this->getRecordSetItemID();
        return $returnvalue;
   }
   public function NumberOfPrimaryKeyFields() { // returns the number of primary key fields defined for this table 
        return 1;
   }

   // Constants holding the mysqli field type character (s,i,d) for each field
  const C_RecordSetItemIDMYSQLI_TYPE = 'i';
  const C_RecordIdMYSQLI_TYPE = 'i';
  const C_RecordSetIDMYSQLI_TYPE = 'i';

   // function to obtain the mysqli field type character from a fieldname
   public function MySQLiFieldType($aFieldname) { 
      $retval = '';
      if ($aFieldname=='RecordSetItemID') { $retval = self::C_RecordSetItemIDMYSQLI_TYPE; }
      if ($aFieldname=='RecordId') { $retval = self::C_RecordIdMYSQLI_TYPE; }
      if ($aFieldname=='RecordSetID') { $retval = self::C_RecordSetIDMYSQLI_TYPE; }
      return $retval;
   }

   // Function load() can take either the value of the primary key which uniquely identifies a particular row
   // or an array of array('primarykeyfieldname'=>'value') in the case of a single field primary key
   // or an array of fieldname value pairs in the case of multiple field primary key.
   public function load($pk) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        try {
             if (is_array($pk)) { 
                 $this->setRecordSetItemID($pk[RecordSetItemID]);
             } else { ;
                 $this->setRecordSetItemID($pk);
             };
        } 
        catch (Exception $e) { 
             throw new Exception($e->getMessage());
        }
        if($this->RecordSetItemID != NULL) {
           $sql = 'SELECT RecordSetItemID, RecordId, RecordSetID FROM recordsetitem WHERE RecordSetItemID = '.$this->RecordSetItemID ;

           $preparesql = 'SELECT RecordSetItemID, RecordId, RecordSetID FROM recordsetitem WHERE RecordSetItemID = ? ';

           if ($statement = $connection->prepare($preparesql)) { 
              $statement->bind_param("i", $this->RecordSetItemID);
              $statement->execute();
              $statement->bind_result($this->RecordSetItemID, $this->RecordId, $this->RecordSetID);
              $statement->fetch();
              $statement->close();
           }

            $this->loaded = true;
            $this->dirty = false;
        } else { 
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   // Function save() will either save the current record or insert a new record.
   // Inserts new record if the primary key field in this table is null 
   // for this instance of this object.
   // Otherwise updates the record identified by the primary key value.
   public function save() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        // Test to see if this is an insert or update.
        if ($this->RecordSetItemID!= NULL) {
            $sql  = 'UPDATE  recordsetitem SET ';
            $isInsert = false;
            $sql .=  "RecordId = ? ";
            $sql .=  ", RecordSetID = ? ";

            $sql .= "  WHERE RecordSetItemID = ? ";
        } else {
            $sql  = 'INSERT INTO recordsetitem ';
            $isInsert = true;
if ($this->PK==NULL) { throw new Exception('Can\'t insert record with null primary key for this table'); }
            $sql .= '( RecordSetItemID ,  RecordId ,  RecordSetID ) VALUES (';
            $sql .=  "  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .= ')';

        }
        if ($statement = $connection->prepare($sql)) { 
           if ($this->RecordSetItemID!= NULL ) {
              $statement->bind_param("iiii", $this->RecordSetItemID , $this->RecordId , $this->RecordSetID , $this->RecordSetItemID );
           } else { 
              $statement->bind_param("iii", $this->RecordSetItemID , $this->RecordId , $this->RecordSetID );
           } 
           $statement->execute();
           if ($statement->num_rows()!=1) {
               $this->error = $statement->error; 
           }
           $statement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }
        if ($this->error=='') { 
            $returnvalue = true;
        };

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function delete() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        if($this->RecordSetItemID != NULL) {
           $sql = 'SELECT RecordSetItemID, RecordId, RecordSetID FROM recordsetitem WHERE RecordSetItemID = "'.$this->RecordSetItemID.'"  ' ;

           $preparedsql = 'SELECT  FROM recordsetitem WHERE  and RecordSetItemID = ?  ' ;
        if ($statement = $connection->prepare($preparedsql)) { 
           $statement->bind_param("iii", $this->RecordSetItemID, $this->RecordId, $this->RecordSetID);
           $statement->execute();
           $statement->store_result();
           if ($statement->num_rows()==1) {
                $sql = 'DELETE FROM recordsetitem WHERE  and RecordSetItemID = ?  ';
                if ($stmt_delete = $connection->prepare($sql)) { 
                   $stmt_delete->bind_param("iii", $this->RecordSetItemID, $this->RecordId, $this->RecordSetID);
                   if ($stmnt_delete->execute()) { 
                       $returnvalue = true;
                   } else {
                       $this->error = mysqli_error($connection); 
                   }
                   $stmt_delete->close();
                }
           } else { 
               $this->error = mysqli_error($connection); 
           }
           $tatement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }

            $this->loaded = true;
            // record was deleted, so set PK to null
            $this->RecordSetItemID = NULL; 
        } else { 
           throw new Exception('Unable to identify which record to delete, primary key is not set');
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function count() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        $sql = 'SELECT count(*)  FROM recordsetitem';
        if ($result = $connection->query($sql)) { 
           if ($result->num_rows()==1) {
             $row = $result->fetch_row();
             if ($row) {
                $returnvalue = $row[0];
             }
           }
        } else { 
           $this->error = mysqli_error($connection); 
        }
        mysqli_free_result($result);

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function loadArrayKeyValueSearch($searchTermArray) {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = array();
       $and = '';
       $wherebit = 'WHERE ';
       foreach($searchTermArray as $fieldname => $searchTerm) {
           if ($this->hasField($fieldname)) { 
               $operator = '='; 
               // change to a like search if a wildcard character is present
               if (!(strpos($searchTerm,'%')===false)) { $operator = 'like'; }
               if (!(strpos($searchTerm,'_')===false)) { $operator = 'like'; }
               if ($searchTerm=='[NULL]') { 
                   $wherebit .= "$and ($fieldname is null or $fieldname='') "; 
               } else { 
                   $wherebit .= "$and $fieldname $operator ? ";
                   $types = $types . $this->MySQLiFieldType($fieldname);
               } 
               $and = ' and ';
           }
       }
       $sql = "SELECT RecordSetItemID FROM recordsetitem $wherebit";
       if ($wherebit=='') { 
             $this->error = 'Error: No search terms provided';
       } else {
          $statement = $connection->prepare($sql);
          $vars = Array();
          $vars[] = $types;
          $i = 0;
          foreach ($searchTermArray as $value) { 
               $varname = 'bind'.$i;  // create a variable name
               $$varname = $value;    // using that variable name store the value 
               $vars[] = &$$varname;  // add a reference to the variable to the array
               $i++;
           }
           //$vars[] contains $types followed by references to variables holding each value in $searchTermArray.
          call_user_func_array(array($statement,'bind_param'),$vars);
          //$statement->bind_param($types,$names);
          $statement->execute();
          $statement->bind_result($id);
          $ids = array();
          while ($statement->fetch()) {
              $ids[] = $id;
          } // double loop to allow all data to be retrieved before preparing a new statement. 
          $statement->close();
          for ($i=0;$i<count($ids);$i++) {
              $obj = new huh_recordsetitem();
              $obj->load($ids[$i]);
              $returnvalue[] = $obj;
              $result=true;
          }
          if ($result===false) { $this->error = mysqli_error($connection); }
       }
       return $returnvalue;
   }	

   //---------------------------------------------------------------------------

// TODO: *************** link to related tables 

   //---------------------------------------------------------------------------

  public function loadLinkedTo() { 
     $returnvalue = array(); 
       // fk: RecordSetID
      $t = new recordset();
      $t->load(getRecordSetID());
      $returnvalue[RecordSetID] = $t;
     return $returnvalue;
  } 
   // Returns an array of primary key values (id) and concatenated values of all other fields (fields)
   // wrapped as $values in: '{ "identifier":"id", "items": [ '.$values.' ] }';
   // when druid_handler.php is called with druid_action=returnfkjson
   // Used with dojo dijit.form.FilteringSelect to submit surrogate numeric key values from a text picklist 
   public function keySelectAllConcatJSON($orderby='ASC') {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
       $returnvalue = '';
       $order = '';
       if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
       $sql = "SELECT RecordSetItemID, concat(IFNULL(RecordId,'') || ' ' || IFNULL(RecordSetID,'')) FROM recordsetitem order by RecordSetID $order ";
       $comma = '';
       if ($result = $connection->query($sql)) { 
          while ($row = $result->fetch_row()) {
             if ($row) {
                $pkval = trim($row[0]);
                $fval = trim($row[1]);
                if ($pkval!='') { 
                    $pkval = str_replace('"','&quot;',$pkval);
                    $fval = str_replace('"','&quot;',$fval);
                    $returnvalue .= $comma . ' { "id":"'.$pkval.'", "fields": "'.$fval.'" } ';
                    $comma = ', ';
                }
             }
          }
       } else { 
          $this->error = mysqli_error(); 
       }
       $result->close();
       return $returnvalue;
   }
   //---------------------------------------------------------------------------

   public function keySelectDistinctJSON($field,$orderby='ASC') {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = '';
       if ($this->hasField($field)) { 
          $order = '';
          $fieldesc = mysql_escape_string($field);
          if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
          $preparemysql = "SELECT DISTINCT $fieldesc FROM recordsetitem order by $fieldesc $order ";
          $comma = '';
         if ($stmt = $connection->prepare($preparemysql)) { 
             $stmt->execute();
             $stmt->bind_result($val);
             while ($stmt->fetch()) {
                $val = trim($val);                if ($val!='') { 
                    $val = str_replace('"','&quot;',$val);
                    $returnvalue .= $comma . ' { "'.$field.'":"'.$val.'" } ';
                    $comma = ', ';
                 }
             }
             $stmt->close();
         }
       }
       return $returnvalue;
   }
   //---------------------------------------------------------------------------


   //---------------------------------------------------------------------------

   // Each field with an index has a load array method generated for it.
   public function loadArrayByRecordSetID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT RecordSetItemID FROM recordsetitem WHERE RecordSetID $operator '$searchTerm'";
        $preparedsql = "SELECT RecordSetItemID FROM recordsetitem WHERE RecordSetID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_recordsetitem();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }

   //---------------------------------------------------------------------------

   // Each fulltext index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each field with an index has a select distinct method generated for it.
   public function selectDistinctRecordSetID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' RecordSetID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, RecordSetID FROM recordsetitem group by RecordSetID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&RecordSetID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }

   public function keySelectDistinct($fieldname,$startline,$link,$endline,$includecount=false,$orderbycount=false) {
       $returnvalue = '';
       switch ($fieldname) { 
          case 'RecordSetID':
             $returnvalue = $this->selectDistinctRecordSetID($startline,$link,$endline,$includecount,$orderbycount);
             break;
       }
       return $returnvalue;
    }

   //---------------------------------------------------------------------------

   public function hasField($fieldname) {
       $returnvalue = false;
       if (trim($fieldname)!='' && trim($fieldname)!=',') {
            if (strpos(self::FIELDLIST," $fieldname, ")!==false) { 
               $returnvalue = true;
            }
       }
       return $returnvalue;
    }
   //---------------------------------------------------------------------------

}


// Write your own views by extending this class.
// Place your extended views in a separate file and you
// can use Druid to regenerate the recordsetitem.php file to reflect changes
// in the underlying database without overwriting your custom views of the data.
// 
class huh_recordsetitemView implements viewer
{
   var $model = null;
   public function setModel($aModel) { 
       $this->model = $aModel;
   }
   // @param $includeRelated default true shows rows from other tables through foreign key relationships.
   // @param $editLinkURL default '' allows adding a link to show this record in an editing form.
   public function getDetailsView($includeRelated=true, $editLinkURL='') {
       $returnvalue = '<ul>';
       $editLinkURL=trim($editLinkURL);
       $model = $this->model;
       $primarykeys = $model->schemaPK();
       if ($editLinkURL!='') { 
          if (!preg_match('/\&$/',$editLinkURL)) { $editLinkURL .= '&'; } 
          $nullpk = false; 
          foreach ($primarykeys as $primarykey) { 
              // Add fieldname=value pairs for primary key(s) to editLinkURL. 
              $editLinkURL .= urlencode($primarykey) . '=' . urlencode($model->keyGet($primarykey));
              if ($model->keyGet($primarykey)=='') { $nullpk = true; } 
          }
          if (!$nullpk) { $returnvalue .= "<li>huh_recordsetitem <a href='$editLinkURL'>Edit</a></li>\n";  } 
       }
       $returnvalue .= "<li>".huh_recordsetitem::RECORDSETITEMID.": ".$model->getRecordSetItemID()."</li>\n";
       $returnvalue .= "<li>".huh_recordsetitem::RECORDID.": ".$model->getRecordId()."</li>\n";
       $returnvalue .= "<li>".huh_recordsetitem::RECORDSETID.": ".$model->getRecordSetID()."</li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
           $returnvalue .= "<li>recordset</li>";
           $t_recordset = new huh_recordset();
           $t_recordsetView = new huh_recordsetView();
           $t_recordsetView->setModel($t_recordset);
           if ($model->getRecordSetID() != '') { 
               $t_recordset->load($model->getRecordSetID());
               $returnvalue .= $t_recordsetView->getDetailsView(false);
           }

        }
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getJSON() {
       $returnvalue = '{ ';
       $model = $this->model;
       $returnvalue .= '"'.huh_recordsetitem::RECORDSETITEMID.': "'.$model->getRecordSetItemID().'",';
       $returnvalue .= '"'.huh_recordsetitem::RECORDID.': "'.$model->getRecordId().'",';
       $returnvalue .= '"'.huh_recordsetitem::RECORDSETID.': "'.$model->getRecordSetID().'" }';
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getTableRowView() {
       $returnvalue = '<tr>';
       $model = $this->model;
       $returnvalue .= "<td>".$model->getRecordSetItemID()."</td>\n";
       $returnvalue .= "<td>".$model->getRecordId()."</td>\n";
       $returnvalue .= "<td>".$model->getRecordSetID()."</td>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getHeaderRow() {
       $returnvalue = '<tr>';
       $returnvalue .= "<th>".huh_recordsetitem::RECORDSETITEMID."</th>\n";
       $returnvalue .= "<th>".huh_recordsetitem::RECORDID."</th>\n";
       $returnvalue .= "<th>".huh_recordsetitem::RECORDSETID."</th>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getEditFormDojoView($includeRelated=true) {
       $model = $this->model;
       if ($model->PK()=='') { $addform=true; } else { $addform=false; } 
       $returnvalue = '';
       $id = trim($model->PK());
       $feedback = "status$id";
       $formname = "editform$id";
       $script = '
  var saveprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function save'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Saving...";
     dojo.xhrGet(saveprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_recordsetitem">';
       $returnvalue .= "<div id='div_$formnane' >";
       if($addform) { 
          $returnvalue .= "Add a new RecordSetItemID"; 
       } else { 
         $returnvalue .= "<div><label for=".huh_recordsetitem::RECORDSETITEMID.">RECORDSETITEMID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_recordsetitem::RECORDSETITEMID." id=".huh_recordsetitem::RECORDSETITEMID." value='".$model->getRecordSetItemID()."'  style=' width:".huh_recordsetitem::RECORDSETITEMID_SIZE ."em;  '  maxlength='".huh_recordsetitem::RECORDSETITEMID_SIZE ."' ></div>\n";
       }  
       $returnvalue .= "<div><label for=".huh_recordsetitem::RECORDID.">RECORDID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_recordsetitem::RECORDID." id=".huh_recordsetitem::RECORDID." value='".$model->getRecordId()."'  style=' width:".huh_recordsetitem::RECORDID_SIZE ."em;  '  maxlength='".huh_recordsetitem::RECORDID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_recordsetitem::RECORDSETID.">RECORDSETID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_recordsetitem::RECORDSETID." id=".huh_recordsetitem::RECORDSETID." value='".$model->getRecordSetID()."'  style=' width:".huh_recordsetitem::RECORDSETID_SIZE ."em;  '  maxlength='".huh_recordsetitem::RECORDSETID_SIZE ."' ></div>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= "<li><input name=save id=save type=button value='Save' onclick='$script  save$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</div>';
       $returnvalue .= '</form>';
       if(!$addform) { 
          // show delete button if editing an existing record 
       $id = trim($model->PK());
       $feedback = "deletestatus$id";
       $formname = "deleteform$id";
       $script = '
  var deleteprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function deleterecord'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Deleting...";
     dojo.xhrGet(deleteprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=delete>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_table value="huh_recordsetitem">';
       $returnvalue .= '<input type=hidden name=RecordSetItemID id=druid_table value="'.$id.'">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li><input name=delete id=save type=button value='Delete' onclick='$script  deleterecord$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       } 
       return  $returnvalue;
   }
   public function getEditFormView($includeRelated=true) {
       $model = $this->model;
       $returnvalue = '<form method=get action=druid_handler.php>';
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_recordsetitem">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li>RECORDSETITEMID<input type=text name=".huh_recordsetitem::RECORDSETITEMID." id=".huh_recordsetitem::RECORDSETITEMID." value='".$model->getRecordSetItemID()."'  size='".huh_recordsetitem::RECORDSETITEMID_SIZE ."'  maxlength='".huh_recordsetitem::RECORDSETITEMID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>RECORDID<input type=text name=".huh_recordsetitem::RECORDID." id=".huh_recordsetitem::RECORDID." value='".$model->getRecordId()."'  size='".huh_recordsetitem::RECORDID_SIZE ."'  maxlength='".huh_recordsetitem::RECORDID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>RECORDSETID<input type=text name=".huh_recordsetitem::RECORDSETID." id=".huh_recordsetitem::RECORDSETID." value='".$model->getRecordSetID()."'  size='".huh_recordsetitem::RECORDSETID_SIZE ."'  maxlength='".huh_recordsetitem::RECORDSETID_SIZE ."' ></li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= '<li><input type=submit value="Save"></li>';
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       return  $returnvalue;
   }
}

//==============================================================================
?>
