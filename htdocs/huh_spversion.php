<?php
//==============================================================================
//===   spversion.php                         
//===   Autogenerated by Druid from MySQL db Build:6
//==============================================================================

include_once("druid_interfaces.php");

class huh_spversion implements  model , loadableModel, saveableModel, tableSchema 
{
   // These constants hold the sizes the fields in this table in the database.
   const SPVERSIONID_SIZE     = 11; //INTEGER
   const TIMESTAMPCREATED_SIZE = 21; //TIMESTAMP
   const TIMESTAMPMODIFIED_SIZE = 21; //TIMESTAMP
   const VERSION_SIZE         = 11; //INTEGER
   const APPNAME_SIZE         = 32; //32
   const APPVERSION_SIZE      = 16; //16
   const SCHEMAVERSION_SIZE   = 16; //16
   const MODIFIEDBYAGENTID_SIZE = 11; //INTEGER
   const CREATEDBYAGENTID_SIZE = 11; //INTEGER
    // These constants hold the field names of the table in the database. 
   const SPVERSIONID       = 'SpVersionID';
   const TIMESTAMPCREATED  = 'TimestampCreated';
   const TIMESTAMPMODIFIED = 'TimestampModified';
   const VERSION           = 'Version';
   const APPNAME           = 'AppName';
   const APPVERSION        = 'AppVersion';
   const SCHEMAVERSION     = 'SchemaVersion';
   const MODIFIEDBYAGENTID = 'ModifiedByAgentID';
   const CREATEDBYAGENTID  = 'CreatedByAgentID';

   //---------------------------------------------------------------------------

   // interface tableSchema implementation
   // schemaPK returns array of primary key field names
   public function schemaPK() {
       return $this->primaryKeyArray;
   } 
   // schemaHaveDistinct returns array of field names for which selectDistinct{fieldname} methods are available.
   public function schemaHaveDistinct() {
       return $this->selectDistinctFieldsArray;
   } 
   // schemaFields returns array of all field names
   public function schemaFields() { 
       return $this->allFieldsArray;
   } 
/*  Example sanitized retrieval of variable matching object variables from $_GET 
/*  Customize these to limit each variable to narrowest possible set of known good values. 

  $SpVersionID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['SpVersionID']), 0, 11);
  $TimestampCreated = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['TimestampCreated']), 0, 21);
  $TimestampModified = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['TimestampModified']), 0, 21);
  $Version = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['Version']), 0, 11);
  $AppName = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['AppName']), 0, 32);
  $AppVersion = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['AppVersion']), 0, 16);
  $SchemaVersion = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['SchemaVersion']), 0, 16);
  $ModifiedByAgentID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['ModifiedByAgentID']), 0, 11);
  $CreatedByAgentID = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['CreatedByAgentID']), 0, 11);
*/

   //---------------------------------------------------------------------------

   private $SpVersionID; // PK INTEGER 
   private $TimestampCreated; // TIMESTAMP 
   private $TimestampModified; // TIMESTAMP 
   private $Version; // INTEGER 
   private $AppName; // VARCHAR(32) 
   private $AppVersion; // VARCHAR(16) 
   private $SchemaVersion; // VARCHAR(16) 
   private $ModifiedByAgentID; // INTEGER 
   private $CreatedByAgentID; // INTEGER 
   private $dirty;
   private $loaded;
   private $error;
   const FIELDLIST = ' SpVersionID, TimestampCreated, TimestampModified, Version, AppName, AppVersion, SchemaVersion, ModifiedByAgentID, CreatedByAgentID, ';
   const PKFIELDLIST = ' SpVersionID, ';
   const NUMBER_OF_PRIMARY_KEYS = 1;
   private $primaryKeyArray = array( 1 => 'SpVersionID'  ) ;
   private $allFieldsArray = array( 0 => 'SpVersionID' , 1 => 'TimestampCreated' , 2 => 'TimestampModified' , 3 => 'Version' , 4 => 'AppName' , 5 => 'AppVersion' , 6 => 'SchemaVersion' , 7 => 'ModifiedByAgentID' , 8 => 'CreatedByAgentID'  ) ;
   private $selectDistinctFieldsArray = array( 7 => 'ModifiedByAgentID' , 8 => 'CreatedByAgentID'  ) ;

   //---------------------------------------------------------------------------

   // constructor 
   function huh_spversion(){
       $this->SpVersionID = NULL;
       $this->TimestampCreated = '';
       $this->TimestampModified = '';
       $this->Version = '';
       $this->AppName = '';
       $this->AppVersion = '';
       $this->SchemaVersion = '';
       $this->ModifiedByAgentID = '';
       $this->CreatedByAgentID = '';
       $this->dirty = false;
       $this->loaded = false;
       $this->error = '';
   }

   private function l_addslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = addslashes($value);
      }
      return $retval;
   }
   private function l_stripslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = stripslashes($value);
      }
      return $retval;
   }
   public function isDirty() {
       return $this->dirty;
   }
   public function isLoaded() {
       return $this->loaded;
   }
   public function errorMessage() {
       return $this->error;
   }

   //---------------------------------------------------------------------------

   public function keyValueSet($fieldname,$value) {
       $returnvalue = false;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpVersionID') { $returnvalue = $this->setSpVersionID($value); } 
             if ($fieldname=='TimestampCreated') { $returnvalue = $this->setTimestampCreated($value); } 
             if ($fieldname=='TimestampModified') { $returnvalue = $this->setTimestampModified($value); } 
             if ($fieldname=='Version') { $returnvalue = $this->setVersion($value); } 
             if ($fieldname=='AppName') { $returnvalue = $this->setAppName($value); } 
             if ($fieldname=='AppVersion') { $returnvalue = $this->setAppVersion($value); } 
             if ($fieldname=='SchemaVersion') { $returnvalue = $this->setSchemaVersion($value); } 
             if ($fieldname=='ModifiedByAgentID') { $returnvalue = $this->setModifiedByAgentID($value); } 
             if ($fieldname=='CreatedByAgentID') { $returnvalue = $this->setCreatedByAgentID($value); } 
             $returnvalue = true;
          }
          catch (exception $e) { ;
              $returnvalue = false;
              throw new Exception('Field Set Error'.$e->getMessage()); 
          }
       } else { 
          throw new Exception('No Such field'); 
       }  
       return $returnvalue;
   }
   public function keyGet($fieldname) {
       $returnvalue = null;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='SpVersionID') { $returnvalue = $this->getSpVersionID(); } 
             if ($fieldname=='TimestampCreated') { $returnvalue = $this->getTimestampCreated(); } 
             if ($fieldname=='TimestampModified') { $returnvalue = $this->getTimestampModified(); } 
             if ($fieldname=='Version') { $returnvalue = $this->getVersion(); } 
             if ($fieldname=='AppName') { $returnvalue = $this->getAppName(); } 
             if ($fieldname=='AppVersion') { $returnvalue = $this->getAppVersion(); } 
             if ($fieldname=='SchemaVersion') { $returnvalue = $this->getSchemaVersion(); } 
             if ($fieldname=='ModifiedByAgentID') { $returnvalue = $this->getModifiedByAgentID(); } 
             if ($fieldname=='CreatedByAgentID') { $returnvalue = $this->getCreatedByAgentID(); } 
          }
          catch (exception $e) { ;
              $returnvalue = null;
          }
       }
       return $returnvalue;
   }
/*SpVersionID*/
   public function getSpVersionID() {
       if ($this->SpVersionID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SpVersionID));
       }
   }
   public function setSpVersionID($SpVersionID) {
       if (strlen(preg_replace('/[^0-9]/','',$SpVersionID)) > huh_spversion::SPVERSIONID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $SpVersionID = trim($SpVersionID);
       if (!ctype_digit(strval($SpVersionID)) && trim(strval($SpVersionID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->SpVersionID = $this->l_addslashes($SpVersionID);
       $this->dirty = true;
   }
/*TimestampCreated*/
   public function getTimestampCreated() {
       if ($this->TimestampCreated==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TimestampCreated));
       }
   }
   public function setTimestampCreated($TimestampCreated) {
       if (strlen($TimestampCreated) > huh_spversion::TIMESTAMPCREATED_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->TimestampCreated = $this->l_addslashes($TimestampCreated);
       $this->dirty = true;
   }
/*TimestampModified*/
   public function getTimestampModified() {
       if ($this->TimestampModified==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->TimestampModified));
       }
   }
   public function setTimestampModified($TimestampModified) {
       if (strlen($TimestampModified) > huh_spversion::TIMESTAMPMODIFIED_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->TimestampModified = $this->l_addslashes($TimestampModified);
       $this->dirty = true;
   }
/*Version*/
   public function getVersion() {
       if ($this->Version==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->Version));
       }
   }
   public function setVersion($Version) {
       if (strlen(preg_replace('/[^0-9]/','',$Version)) > huh_spversion::VERSION_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $Version = trim($Version);
       if (!ctype_digit(strval($Version)) && trim(strval($Version))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->Version = $this->l_addslashes($Version);
       $this->dirty = true;
   }
/*AppName*/
   public function getAppName() {
       if ($this->AppName==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->AppName));
       }
   }
   public function setAppName($AppName) {
       if (strlen($AppName) > huh_spversion::APPNAME_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->AppName = $this->l_addslashes($AppName);
       $this->dirty = true;
   }
/*AppVersion*/
   public function getAppVersion() {
       if ($this->AppVersion==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->AppVersion));
       }
   }
   public function setAppVersion($AppVersion) {
       if (strlen($AppVersion) > huh_spversion::APPVERSION_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->AppVersion = $this->l_addslashes($AppVersion);
       $this->dirty = true;
   }
/*SchemaVersion*/
   public function getSchemaVersion() {
       if ($this->SchemaVersion==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SchemaVersion));
       }
   }
   public function setSchemaVersion($SchemaVersion) {
       if (strlen($SchemaVersion) > huh_spversion::SCHEMAVERSION_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->SchemaVersion = $this->l_addslashes($SchemaVersion);
       $this->dirty = true;
   }
/*ModifiedByAgentID*/
   public function getModifiedByAgentID() {
       if ($this->ModifiedByAgentID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->ModifiedByAgentID));
       }
   }
   public function setModifiedByAgentID($ModifiedByAgentID) {
       if (strlen(preg_replace('/[^0-9]/','',$ModifiedByAgentID)) > huh_spversion::MODIFIEDBYAGENTID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $ModifiedByAgentID = trim($ModifiedByAgentID);
       if (!ctype_digit(strval($ModifiedByAgentID)) && trim(strval($ModifiedByAgentID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->ModifiedByAgentID = $this->l_addslashes($ModifiedByAgentID);
       $this->dirty = true;
   }
/*CreatedByAgentID*/
   public function getCreatedByAgentID() {
       if ($this->CreatedByAgentID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->CreatedByAgentID));
       }
   }
   public function setCreatedByAgentID($CreatedByAgentID) {
       if (strlen(preg_replace('/[^0-9]/','',$CreatedByAgentID)) > huh_spversion::CREATEDBYAGENTID_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $CreatedByAgentID = trim($CreatedByAgentID);
       if (!ctype_digit(strval($CreatedByAgentID)) && trim(strval($CreatedByAgentID))!='' ) {
             throw new Exception("Value must be an integer");
       }
       $this->CreatedByAgentID = $this->l_addslashes($CreatedByAgentID);
       $this->dirty = true;
   }
   public function PK() { // get value of primary key 
        $returnvalue = '';
        $returnvalue .= $this->getSpVersionID();
        return $returnvalue;
   }
   public function PKArray() { // get name and value of primary key fields 
        $returnvalue = array();
        $returnvalue['SpVersionID'] = $this->getSpVersionID();
        return $returnvalue;
   }
   public function NumberOfPrimaryKeyFields() { // returns the number of primary key fields defined for this table 
        return 1;
   }

   // Constants holding the mysqli field type character (s,i,d) for each field
  const C_SpVersionIDMYSQLI_TYPE = 'i';
  const C_TimestampCreatedMYSQLI_TYPE = 's';
  const C_TimestampModifiedMYSQLI_TYPE = 's';
  const C_VersionMYSQLI_TYPE = 'i';
  const C_AppNameMYSQLI_TYPE = 's';
  const C_AppVersionMYSQLI_TYPE = 's';
  const C_SchemaVersionMYSQLI_TYPE = 's';
  const C_ModifiedByAgentIDMYSQLI_TYPE = 'i';
  const C_CreatedByAgentIDMYSQLI_TYPE = 'i';

   // function to obtain the mysqli field type character from a fieldname
   public function MySQLiFieldType($aFieldname) { 
      $retval = '';
      if ($aFieldname=='SpVersionID') { $retval = self::C_SpVersionIDMYSQLI_TYPE; }
      if ($aFieldname=='TimestampCreated') { $retval = self::C_TimestampCreatedMYSQLI_TYPE; }
      if ($aFieldname=='TimestampModified') { $retval = self::C_TimestampModifiedMYSQLI_TYPE; }
      if ($aFieldname=='Version') { $retval = self::C_VersionMYSQLI_TYPE; }
      if ($aFieldname=='AppName') { $retval = self::C_AppNameMYSQLI_TYPE; }
      if ($aFieldname=='AppVersion') { $retval = self::C_AppVersionMYSQLI_TYPE; }
      if ($aFieldname=='SchemaVersion') { $retval = self::C_SchemaVersionMYSQLI_TYPE; }
      if ($aFieldname=='ModifiedByAgentID') { $retval = self::C_ModifiedByAgentIDMYSQLI_TYPE; }
      if ($aFieldname=='CreatedByAgentID') { $retval = self::C_CreatedByAgentIDMYSQLI_TYPE; }
      return $retval;
   }

   // Function load() can take either the value of the primary key which uniquely identifies a particular row
   // or an array of array('primarykeyfieldname'=>'value') in the case of a single field primary key
   // or an array of fieldname value pairs in the case of multiple field primary key.
   public function load($pk) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        try {
             if (is_array($pk)) { 
                 $this->setSpVersionID($pk[SpVersionID]);
             } else { ;
                 $this->setSpVersionID($pk);
             };
        } 
        catch (Exception $e) { 
             throw new Exception($e->getMessage());
        }
        if($this->SpVersionID != NULL) {
           $sql = 'SELECT SpVersionID, TimestampCreated, TimestampModified, Version, AppName, AppVersion, SchemaVersion, ModifiedByAgentID, CreatedByAgentID FROM spversion WHERE SpVersionID = '.$this->SpVersionID ;

           $preparesql = 'SELECT SpVersionID, TimestampCreated, TimestampModified, Version, AppName, AppVersion, SchemaVersion, ModifiedByAgentID, CreatedByAgentID FROM spversion WHERE SpVersionID = ? ';

           if ($statement = $connection->prepare($preparesql)) { 
              $statement->bind_param("i", $this->SpVersionID);
              $statement->execute();
              $statement->bind_result($this->SpVersionID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->AppName, $this->AppVersion, $this->SchemaVersion, $this->ModifiedByAgentID, $this->CreatedByAgentID);
              $statement->fetch();
              $statement->close();
           }

            $this->loaded = true;
            $this->dirty = false;
        } else { 
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   // Function save() will either save the current record or insert a new record.
   // Inserts new record if the primary key field in this table is null 
   // for this instance of this object.
   // Otherwise updates the record identified by the primary key value.
   public function save() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        // Test to see if this is an insert or update.
        if ($this->SpVersionID!= NULL) {
            $sql  = 'UPDATE  spversion SET ';
            $isInsert = false;
            $sql .=  "TimestampCreated = ? ";
            $sql .=  ", TimestampModified = ? ";
            $sql .=  ", Version = ? ";
            $sql .=  ", AppName = ? ";
            $sql .=  ", AppVersion = ? ";
            $sql .=  ", SchemaVersion = ? ";
            $sql .=  ", ModifiedByAgentID = ? ";
            $sql .=  ", CreatedByAgentID = ? ";

            $sql .= "  WHERE SpVersionID = ? ";
        } else {
            $sql  = 'INSERT INTO spversion ';
            $isInsert = true;
if ($this->PK==NULL) { throw new Exception('Can\'t insert record with null primary key for this table'); }
            $sql .= '( SpVersionID ,  TimestampCreated ,  TimestampModified ,  Version ,  AppName ,  AppVersion ,  SchemaVersion ,  ModifiedByAgentID ,  CreatedByAgentID ) VALUES (';
            $sql .=  "  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .= ')';

        }
        if ($statement = $connection->prepare($sql)) { 
           if ($this->SpVersionID!= NULL ) {
              $statement->bind_param("ississsiii", $this->SpVersionID , $this->TimestampCreated , $this->TimestampModified , $this->Version , $this->AppName , $this->AppVersion , $this->SchemaVersion , $this->ModifiedByAgentID , $this->CreatedByAgentID , $this->SpVersionID );
           } else { 
              $statement->bind_param("ississsii", $this->SpVersionID , $this->TimestampCreated , $this->TimestampModified , $this->Version , $this->AppName , $this->AppVersion , $this->SchemaVersion , $this->ModifiedByAgentID , $this->CreatedByAgentID );
           } 
           $statement->execute();
           if ($statement->num_rows()!=1) {
               $this->error = $statement->error; 
           }
           $statement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }
        if ($this->error=='') { 
            $returnvalue = true;
        };

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function delete() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        if($this->SpVersionID != NULL) {
           $sql = 'SELECT SpVersionID, TimestampCreated, TimestampModified, Version, AppName, AppVersion, SchemaVersion, ModifiedByAgentID, CreatedByAgentID FROM spversion WHERE SpVersionID = "'.$this->SpVersionID.'"  ' ;

           $preparedsql = 'SELECT  FROM spversion WHERE  and SpVersionID = ?  ' ;
        if ($statement = $connection->prepare($preparedsql)) { 
           $statement->bind_param("ississsii", $this->SpVersionID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->AppName, $this->AppVersion, $this->SchemaVersion, $this->ModifiedByAgentID, $this->CreatedByAgentID);
           $statement->execute();
           $statement->store_result();
           if ($statement->num_rows()==1) {
                $sql = 'DELETE FROM spversion WHERE  and SpVersionID = ?  ';
                if ($stmt_delete = $connection->prepare($sql)) { 
                   $stmt_delete->bind_param("ississsii", $this->SpVersionID, $this->TimestampCreated, $this->TimestampModified, $this->Version, $this->AppName, $this->AppVersion, $this->SchemaVersion, $this->ModifiedByAgentID, $this->CreatedByAgentID);
                   if ($stmnt_delete->execute()) { 
                       $returnvalue = true;
                   } else {
                       $this->error = mysqli_error($connection); 
                   }
                   $stmt_delete->close();
                }
           } else { 
               $this->error = mysqli_error($connection); 
           }
           $tatement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }

            $this->loaded = true;
            // record was deleted, so set PK to null
            $this->SpVersionID = NULL; 
        } else { 
           throw new Exception('Unable to identify which record to delete, primary key is not set');
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function count() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        $sql = 'SELECT count(*)  FROM spversion';
        if ($result = $connection->query($sql)) { 
           if ($result->num_rows()==1) {
             $row = $result->fetch_row();
             if ($row) {
                $returnvalue = $row[0];
             }
           }
        } else { 
           $this->error = mysqli_error($connection); 
        }
        mysqli_free_result($result);

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function loadArrayKeyValueSearch($searchTermArray) {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = array();
       $and = '';
       $wherebit = 'WHERE ';
       foreach($searchTermArray as $fieldname => $searchTerm) {
           if ($this->hasField($fieldname)) { 
               $operator = '='; 
               // change to a like search if a wildcard character is present
               if (!(strpos($searchTerm,'%')===false)) { $operator = 'like'; }
               if (!(strpos($searchTerm,'_')===false)) { $operator = 'like'; }
               if ($searchTerm=='[NULL]') { 
                   $wherebit .= "$and ($fieldname is null or $fieldname='') "; 
               } else { 
                   $wherebit .= "$and $fieldname $operator ? ";
                   $types = $types . $this->MySQLiFieldType($fieldname);
               } 
               $and = ' and ';
           }
       }
       $sql = "SELECT SpVersionID FROM spversion $wherebit";
       if ($wherebit=='') { 
             $this->error = 'Error: No search terms provided';
       } else {
          $statement = $connection->prepare($sql);
          $vars = Array();
          $vars[] = $types;
          $i = 0;
          foreach ($searchTermArray as $value) { 
               $varname = 'bind'.$i;  // create a variable name
               $$varname = $value;    // using that variable name store the value 
               $vars[] = &$$varname;  // add a reference to the variable to the array
               $i++;
           }
           //$vars[] contains $types followed by references to variables holding each value in $searchTermArray.
          call_user_func_array(array($statement,'bind_param'),$vars);
          //$statement->bind_param($types,$names);
          $statement->execute();
          $statement->bind_result($id);
          $ids = array();
          while ($statement->fetch()) {
              $ids[] = $id;
          } // double loop to allow all data to be retrieved before preparing a new statement. 
          $statement->close();
          for ($i=0;$i<count($ids);$i++) {
              $obj = new huh_spversion();
              $obj->load($ids[$i]);
              $returnvalue[] = $obj;
              $result=true;
          }
          if ($result===false) { $this->error = mysqli_error($connection); }
       }
       return $returnvalue;
   }	

   //---------------------------------------------------------------------------

// TODO: *************** link to related tables 

   //---------------------------------------------------------------------------

  public function loadLinkedTo() { 
     $returnvalue = array(); 
       // fk: ModifiedByAgentID
      $t = new agent();
      $t->load(getModifiedByAgentID());
      $returnvalue[ModifiedByAgentID] = $t;
       // fk: CreatedByAgentID
      $t = new agent();
      $t->load(getCreatedByAgentID());
      $returnvalue[CreatedByAgentID] = $t;
     return $returnvalue;
  } 
   // Returns an array of primary key values (id) and concatenated values of all other fields (fields)
   // wrapped as $values in: '{ "identifier":"id", "items": [ '.$values.' ] }';
   // when druid_handler.php is called with druid_action=returnfkjson
   // Used with dojo dijit.form.FilteringSelect to submit surrogate numeric key values from a text picklist 
   public function keySelectAllConcatJSON($orderby='ASC') {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
       $returnvalue = '';
       $order = '';
       if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
       $sql = "SELECT SpVersionID, concat(IFNULL(TimestampCreated,'') || ' ' || IFNULL(TimestampModified,'') || ' ' || IFNULL(Version,'') || ' ' || IFNULL(AppName,'') || ' ' || IFNULL(AppVersion,'') || ' ' || IFNULL(SchemaVersion,'') || ' ' || IFNULL(ModifiedByAgentID,'') || ' ' || IFNULL(CreatedByAgentID,'')) FROM spversion order by ModifiedByAgentID, CreatedByAgentID $order ";
       $comma = '';
       if ($result = $connection->query($sql)) { 
          while ($row = $result->fetch_row()) {
             if ($row) {
                $pkval = trim($row[0]);
                $fval = trim($row[1]);
                if ($pkval!='') { 
                    $pkval = str_replace('"','&quot;',$pkval);
                    $fval = str_replace('"','&quot;',$fval);
                    $returnvalue .= $comma . ' { "id":"'.$pkval.'", "fields": "'.$fval.'" } ';
                    $comma = ', ';
                }
             }
          }
       } else { 
          $this->error = mysqli_error(); 
       }
       $result->close();
       return $returnvalue;
   }
   //---------------------------------------------------------------------------

   public function keySelectDistinctJSON($field,$orderby='ASC') {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = '';
       if ($this->hasField($field)) { 
          $order = '';
          $fieldesc = mysql_escape_string($field);
          if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
          $preparemysql = "SELECT DISTINCT $fieldesc FROM spversion order by $fieldesc $order ";
          $comma = '';
         if ($stmt = $connection->prepare($preparemysql)) { 
             $stmt->execute();
             $stmt->bind_result($val);
             while ($stmt->fetch()) {
                $val = trim($val);                if ($val!='') { 
                    $val = str_replace('"','&quot;',$val);
                    $returnvalue .= $comma . ' { "'.$field.'":"'.$val.'" } ';
                    $comma = ', ';
                 }
             }
             $stmt->close();
         }
       }
       return $returnvalue;
   }
   //---------------------------------------------------------------------------


   //---------------------------------------------------------------------------

   // Each field with an index has a load array method generated for it.
   public function loadArrayByModifiedByAgentID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT SpVersionID FROM spversion WHERE ModifiedByAgentID $operator '$searchTerm'";
        $preparedsql = "SELECT SpVersionID FROM spversion WHERE ModifiedByAgentID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_spversion();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }
   public function loadArrayByCreatedByAgentID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT SpVersionID FROM spversion WHERE CreatedByAgentID $operator '$searchTerm'";
        $preparedsql = "SELECT SpVersionID FROM spversion WHERE CreatedByAgentID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_spversion();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }

   //---------------------------------------------------------------------------

   // Each fulltext index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each field with an index has a select distinct method generated for it.
   public function selectDistinctModifiedByAgentID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' ModifiedByAgentID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, ModifiedByAgentID FROM spversion group by ModifiedByAgentID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&ModifiedByAgentID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }
   public function selectDistinctCreatedByAgentID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' CreatedByAgentID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, CreatedByAgentID FROM spversion group by CreatedByAgentID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&CreatedByAgentID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }

   public function keySelectDistinct($fieldname,$startline,$link,$endline,$includecount=false,$orderbycount=false) {
       $returnvalue = '';
       switch ($fieldname) { 
          case 'ModifiedByAgentID':
             $returnvalue = $this->selectDistinctModifiedByAgentID($startline,$link,$endline,$includecount,$orderbycount);
             break;
          case 'CreatedByAgentID':
             $returnvalue = $this->selectDistinctCreatedByAgentID($startline,$link,$endline,$includecount,$orderbycount);
             break;
       }
       return $returnvalue;
    }

   //---------------------------------------------------------------------------

   public function hasField($fieldname) {
       $returnvalue = false;
       if (trim($fieldname)!='' && trim($fieldname)!=',') {
            if (strpos(self::FIELDLIST," $fieldname, ")!==false) { 
               $returnvalue = true;
            }
       }
       return $returnvalue;
    }
   //---------------------------------------------------------------------------

}


// Write your own views by extending this class.
// Place your extended views in a separate file and you
// can use Druid to regenerate the spversion.php file to reflect changes
// in the underlying database without overwriting your custom views of the data.
// 
class huh_spversionView implements viewer
{
   var $model = null;
   public function setModel($aModel) { 
       $this->model = $aModel;
   }
   // @param $includeRelated default true shows rows from other tables through foreign key relationships.
   // @param $editLinkURL default '' allows adding a link to show this record in an editing form.
   public function getDetailsView($includeRelated=true, $editLinkURL='') {
       $returnvalue = '<ul>';
       $editLinkURL=trim($editLinkURL);
       $model = $this->model;
       $primarykeys = $model->schemaPK();
       if ($editLinkURL!='') { 
          if (!preg_match('/\&$/',$editLinkURL)) { $editLinkURL .= '&'; } 
          $nullpk = false; 
          foreach ($primarykeys as $primarykey) { 
              // Add fieldname=value pairs for primary key(s) to editLinkURL. 
              $editLinkURL .= urlencode($primarykey) . '=' . urlencode($model->keyGet($primarykey));
              if ($model->keyGet($primarykey)=='') { $nullpk = true; } 
          }
          if (!$nullpk) { $returnvalue .= "<li>huh_spversion <a href='$editLinkURL'>Edit</a></li>\n";  } 
       }
       $returnvalue .= "<li>".huh_spversion::SPVERSIONID.": ".$model->getSpVersionID()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::TIMESTAMPCREATED.": ".$model->getTimestampCreated()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::TIMESTAMPMODIFIED.": ".$model->getTimestampModified()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::VERSION.": ".$model->getVersion()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::APPNAME.": ".$model->getAppName()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::APPVERSION.": ".$model->getAppVersion()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::SCHEMAVERSION.": ".$model->getSchemaVersion()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::MODIFIEDBYAGENTID.": ".$model->getModifiedByAgentID()."</li>\n";
       $returnvalue .= "<li>".huh_spversion::CREATEDBYAGENTID.": ".$model->getCreatedByAgentID()."</li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
           $returnvalue .= "<li>agent</li>";
           $t_agent = new huh_agent();
           $t_agentView = new huh_agentView();
           $t_agentView->setModel($t_agent);
           if ($model->getModifiedByAgentID() != '') { 
               $t_agent->load($model->getModifiedByAgentID());
               $returnvalue .= $t_agentView->getDetailsView(false);
           }
           $returnvalue .= "<li>agent</li>";
           $t_agent = new huh_agent();
           $t_agentView = new huh_agentView();
           $t_agentView->setModel($t_agent);
           if ($model->getCreatedByAgentID() != '') { 
               $t_agent->load($model->getCreatedByAgentID());
               $returnvalue .= $t_agentView->getDetailsView(false);
           }

        }
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getJSON() {
       $returnvalue = '{ ';
       $model = $this->model;
       $returnvalue .= '"'.huh_spversion::SPVERSIONID.': "'.$model->getSpVersionID().'",';
       $returnvalue .= '"'.huh_spversion::TIMESTAMPCREATED.': "'.$model->getTimestampCreated().'",';
       $returnvalue .= '"'.huh_spversion::TIMESTAMPMODIFIED.': "'.$model->getTimestampModified().'",';
       $returnvalue .= '"'.huh_spversion::VERSION.': "'.$model->getVersion().'",';
       $returnvalue .= '"'.huh_spversion::APPNAME.': "'.$model->getAppName().'",';
       $returnvalue .= '"'.huh_spversion::APPVERSION.': "'.$model->getAppVersion().'",';
       $returnvalue .= '"'.huh_spversion::SCHEMAVERSION.': "'.$model->getSchemaVersion().'",';
       $returnvalue .= '"'.huh_spversion::MODIFIEDBYAGENTID.': "'.$model->getModifiedByAgentID().'",';
       $returnvalue .= '"'.huh_spversion::CREATEDBYAGENTID.': "'.$model->getCreatedByAgentID().'" }';
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getTableRowView() {
       $returnvalue = '<tr>';
       $model = $this->model;
       $returnvalue .= "<td>".$model->getSpVersionID()."</td>\n";
       $returnvalue .= "<td>".$model->getTimestampCreated()."</td>\n";
       $returnvalue .= "<td>".$model->getTimestampModified()."</td>\n";
       $returnvalue .= "<td>".$model->getVersion()."</td>\n";
       $returnvalue .= "<td>".$model->getAppName()."</td>\n";
       $returnvalue .= "<td>".$model->getAppVersion()."</td>\n";
       $returnvalue .= "<td>".$model->getSchemaVersion()."</td>\n";
       $returnvalue .= "<td>".$model->getModifiedByAgentID()."</td>\n";
       $returnvalue .= "<td>".$model->getCreatedByAgentID()."</td>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getHeaderRow() {
       $returnvalue = '<tr>';
       $returnvalue .= "<th>".huh_spversion::SPVERSIONID."</th>\n";
       $returnvalue .= "<th>".huh_spversion::TIMESTAMPCREATED."</th>\n";
       $returnvalue .= "<th>".huh_spversion::TIMESTAMPMODIFIED."</th>\n";
       $returnvalue .= "<th>".huh_spversion::VERSION."</th>\n";
       $returnvalue .= "<th>".huh_spversion::APPNAME."</th>\n";
       $returnvalue .= "<th>".huh_spversion::APPVERSION."</th>\n";
       $returnvalue .= "<th>".huh_spversion::SCHEMAVERSION."</th>\n";
       $returnvalue .= "<th>".huh_spversion::MODIFIEDBYAGENTID."</th>\n";
       $returnvalue .= "<th>".huh_spversion::CREATEDBYAGENTID."</th>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getEditFormDojoView($includeRelated=true) {
       $model = $this->model;
       if ($model->PK()=='') { $addform=true; } else { $addform=false; } 
       $returnvalue = '';
       $id = trim($model->PK());
       $feedback = "status$id";
       $formname = "editform$id";
       $script = '
  var saveprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function save'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Saving...";
     dojo.xhrGet(saveprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_spversion">';
       $returnvalue .= "<div id='div_$formnane' >";
       if($addform) { 
          $returnvalue .= "Add a new SpVersionID"; 
       } else { 
         $returnvalue .= "<div><label for=".huh_spversion::SPVERSIONID.">SPVERSIONID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spversion::SPVERSIONID." id=".huh_spversion::SPVERSIONID." value='".$model->getSpVersionID()."'  style=' width:".huh_spversion::SPVERSIONID_SIZE ."em;  '  maxlength='".huh_spversion::SPVERSIONID_SIZE ."' ></div>\n";
       }  
       $returnvalue .= "<div><label for=".huh_spversion::TIMESTAMPCREATED.">TIMESTAMPCREATED</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spversion::TIMESTAMPCREATED." id=".huh_spversion::TIMESTAMPCREATED." value='".$model->getTimestampCreated()."'  style=' width:".huh_spversion::TIMESTAMPCREATED_SIZE ."em;  '  maxlength='".huh_spversion::TIMESTAMPCREATED_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::TIMESTAMPMODIFIED.">TIMESTAMPMODIFIED</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spversion::TIMESTAMPMODIFIED." id=".huh_spversion::TIMESTAMPMODIFIED." value='".$model->getTimestampModified()."'  style=' width:".huh_spversion::TIMESTAMPMODIFIED_SIZE ."em;  '  maxlength='".huh_spversion::TIMESTAMPMODIFIED_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::VERSION.">VERSION</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spversion::VERSION." id=".huh_spversion::VERSION." value='".$model->getVersion()."'  style=' width:".huh_spversion::VERSION_SIZE ."em;  '  maxlength='".huh_spversion::VERSION_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::APPNAME.">APPNAME</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spversion::APPNAME." id=".huh_spversion::APPNAME." value='".$model->getAppName()."'  style=' width:".huh_spversion::APPNAME_SIZE ."em;  '  maxlength='".huh_spversion::APPNAME_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::APPVERSION.">APPVERSION</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spversion::APPVERSION." id=".huh_spversion::APPVERSION." value='".$model->getAppVersion()."'  style=' width:".huh_spversion::APPVERSION_SIZE ."em;  '  maxlength='".huh_spversion::APPVERSION_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::SCHEMAVERSION.">SCHEMAVERSION</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_spversion::SCHEMAVERSION." id=".huh_spversion::SCHEMAVERSION." value='".$model->getSchemaVersion()."'  style=' width:".huh_spversion::SCHEMAVERSION_SIZE ."em;  '  maxlength='".huh_spversion::SCHEMAVERSION_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::MODIFIEDBYAGENTID.">MODIFIEDBYAGENTID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spversion::MODIFIEDBYAGENTID." id=".huh_spversion::MODIFIEDBYAGENTID." value='".$model->getModifiedByAgentID()."'  style=' width:".huh_spversion::MODIFIEDBYAGENTID_SIZE ."em;  '  maxlength='".huh_spversion::MODIFIEDBYAGENTID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_spversion::CREATEDBYAGENTID.">CREATEDBYAGENTID</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_spversion::CREATEDBYAGENTID." id=".huh_spversion::CREATEDBYAGENTID." value='".$model->getCreatedByAgentID()."'  style=' width:".huh_spversion::CREATEDBYAGENTID_SIZE ."em;  '  maxlength='".huh_spversion::CREATEDBYAGENTID_SIZE ."' ></div>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= "<li><input name=save id=save type=button value='Save' onclick='$script  save$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</div>';
       $returnvalue .= '</form>';
       if(!$addform) { 
          // show delete button if editing an existing record 
       $id = trim($model->PK());
       $feedback = "deletestatus$id";
       $formname = "deleteform$id";
       $script = '
  var deleteprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function deleterecord'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Deleting...";
     dojo.xhrGet(deleteprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=delete>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_table value="huh_spversion">';
       $returnvalue .= '<input type=hidden name=SpVersionID id=druid_table value="'.$id.'">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li><input name=delete id=save type=button value='Delete' onclick='$script  deleterecord$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       } 
       return  $returnvalue;
   }
   public function getEditFormView($includeRelated=true) {
       $model = $this->model;
       $returnvalue = '<form method=get action=druid_handler.php>';
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_spversion">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li>SPVERSIONID<input type=text name=".huh_spversion::SPVERSIONID." id=".huh_spversion::SPVERSIONID." value='".$model->getSpVersionID()."'  size='".huh_spversion::SPVERSIONID_SIZE ."'  maxlength='".huh_spversion::SPVERSIONID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TIMESTAMPCREATED<input type=text name=".huh_spversion::TIMESTAMPCREATED." id=".huh_spversion::TIMESTAMPCREATED." value='".$model->getTimestampCreated()."'  size='".huh_spversion::TIMESTAMPCREATED_SIZE ."'  maxlength='".huh_spversion::TIMESTAMPCREATED_SIZE ."' ></li>\n";
       $returnvalue .= "<li>TIMESTAMPMODIFIED<input type=text name=".huh_spversion::TIMESTAMPMODIFIED." id=".huh_spversion::TIMESTAMPMODIFIED." value='".$model->getTimestampModified()."'  size='".huh_spversion::TIMESTAMPMODIFIED_SIZE ."'  maxlength='".huh_spversion::TIMESTAMPMODIFIED_SIZE ."' ></li>\n";
       $returnvalue .= "<li>VERSION<input type=text name=".huh_spversion::VERSION." id=".huh_spversion::VERSION." value='".$model->getVersion()."'  size='".huh_spversion::VERSION_SIZE ."'  maxlength='".huh_spversion::VERSION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>APPNAME<input type=text name=".huh_spversion::APPNAME." id=".huh_spversion::APPNAME." value='".$model->getAppName()."'  size='".huh_spversion::APPNAME_SIZE ."'  maxlength='".huh_spversion::APPNAME_SIZE ."' ></li>\n";
       $returnvalue .= "<li>APPVERSION<input type=text name=".huh_spversion::APPVERSION." id=".huh_spversion::APPVERSION." value='".$model->getAppVersion()."'  size='".huh_spversion::APPVERSION_SIZE ."'  maxlength='".huh_spversion::APPVERSION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>SCHEMAVERSION<input type=text name=".huh_spversion::SCHEMAVERSION." id=".huh_spversion::SCHEMAVERSION." value='".$model->getSchemaVersion()."'  size='".huh_spversion::SCHEMAVERSION_SIZE ."'  maxlength='".huh_spversion::SCHEMAVERSION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>MODIFIEDBYAGENTID<input type=text name=".huh_spversion::MODIFIEDBYAGENTID." id=".huh_spversion::MODIFIEDBYAGENTID." value='".$model->getModifiedByAgentID()."'  size='".huh_spversion::MODIFIEDBYAGENTID_SIZE ."'  maxlength='".huh_spversion::MODIFIEDBYAGENTID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>CREATEDBYAGENTID<input type=text name=".huh_spversion::CREATEDBYAGENTID." id=".huh_spversion::CREATEDBYAGENTID." value='".$model->getCreatedByAgentID()."'  size='".huh_spversion::CREATEDBYAGENTID_SIZE ."'  maxlength='".huh_spversion::CREATEDBYAGENTID_SIZE ."' ></li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= '<li><input type=submit value="Save"></li>';
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       return  $returnvalue;
   }
}

//==============================================================================
?>
