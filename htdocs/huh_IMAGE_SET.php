<?php
//==============================================================================
//===   IMAGE_SET.php                         
//===   Autogenerated by Druid from MySQL db Build:6
//==============================================================================

include_once("druid_interfaces.php");

class huh_IMAGE_SET implements  model , loadableModel, saveableModel, tableSchema 
{
   // These constants hold the sizes the fields in this table in the database.
   const ID_SIZE              = 20; //BIGINT
   const SPECIMEN_ID_SIZE     = 20; //BIGINT
   const BATCH_ID_SIZE        = 20; //BIGINT
   const ACCESS_TYPE_ID_SIZE  = 20; //BIGINT
   const SOURCE_TYPE_ID_SIZE  = 20; //BIGINT
   const ACTIVE_FLAG_SIZE     = 11; //INTEGER
   const DESCRIPTION_SIZE     = 1200; //1200
   const CAPTION_SIZE         = 200; //200
   const OWNER_SIZE           = 200; //200
   const COPYRIGHT_SIZE       = 100; //100
   const REMARKS_SIZE         = 1000; //1000
   const DATA_SOURCE_SIZE     = 30; //30
   const SYS_DATA_SIZE        = 30; //30
    // These constants hold the field names of the table in the database. 
   const ID                = 'ID';
   const SPECIMEN_ID       = 'SPECIMEN_ID';
   const BATCH_ID          = 'BATCH_ID';
   const ACCESS_TYPE_ID    = 'ACCESS_TYPE_ID';
   const SOURCE_TYPE_ID    = 'SOURCE_TYPE_ID';
   const ACTIVE_FLAG       = 'ACTIVE_FLAG';
   const DESCRIPTION       = 'DESCRIPTION';
   const CAPTION           = 'CAPTION';
   const OWNER             = 'OWNER';
   const COPYRIGHT         = 'COPYRIGHT';
   const REMARKS           = 'REMARKS';
   const DATA_SOURCE       = 'DATA_SOURCE';
   const SYS_DATA          = 'SYS_DATA';

   //---------------------------------------------------------------------------

   // interface tableSchema implementation
   // schemaPK returns array of primary key field names
   public function schemaPK() {
       return $this->primaryKeyArray;
   } 
   // schemaHaveDistinct returns array of field names for which selectDistinct{fieldname} methods are available.
   public function schemaHaveDistinct() {
       return $this->selectDistinctFieldsArray;
   } 
   // schemaFields returns array of all field names
   public function schemaFields() { 
       return $this->allFieldsArray;
   } 
/*  Example sanitized retrieval of variable matching object variables from $_GET 
/*  Customize these to limit each variable to narrowest possible set of known good values. 

  $ID = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['ID']), 0, 20);
  $SPECIMEN_ID = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['SPECIMEN_ID']), 0, 20);
  $BATCH_ID = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['BATCH_ID']), 0, 20);
  $ACCESS_TYPE_ID = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['ACCESS_TYPE_ID']), 0, 20);
  $SOURCE_TYPE_ID = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['SOURCE_TYPE_ID']), 0, 20);
  $ACTIVE_FLAG = substr(preg_replace('/[^0-9\-\[NULL\]]/','',$_GET['ACTIVE_FLAG']), 0, 11);
  $DESCRIPTION = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['DESCRIPTION']), 0, 1200);
  $CAPTION = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['CAPTION']), 0, 200);
  $OWNER = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['OWNER']), 0, 200);
  $COPYRIGHT = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['COPYRIGHT']), 0, 100);
  $REMARKS = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['REMARKS']), 0, 1000);
  $DATA_SOURCE = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['DATA_SOURCE']), 0, 30);
  $SYS_DATA = substr(preg_replace('/[^A-Za-z0-9\.\.\ \[NULL\]]/','',$_GET['SYS_DATA']), 0, 30);
*/

   //---------------------------------------------------------------------------

   private $ID; // PK BIGINT 
   private $SPECIMEN_ID; // BIGINT 
   private $BATCH_ID; // BIGINT 
   private $ACCESS_TYPE_ID; // BIGINT 
   private $SOURCE_TYPE_ID; // BIGINT 
   private $ACTIVE_FLAG; // INTEGER 
   private $DESCRIPTION; // VARCHAR(1200) 
   private $CAPTION; // VARCHAR(200) 
   private $OWNER; // VARCHAR(200) 
   private $COPYRIGHT; // VARCHAR(100) 
   private $REMARKS; // VARCHAR(1000) 
   private $DATA_SOURCE; // VARCHAR(30) 
   private $SYS_DATA; // VARCHAR(30) 
   private $dirty;
   private $loaded;
   private $error;
   const FIELDLIST = ' ID, SPECIMEN_ID, BATCH_ID, ACCESS_TYPE_ID, SOURCE_TYPE_ID, ACTIVE_FLAG, DESCRIPTION, CAPTION, OWNER, COPYRIGHT, REMARKS, DATA_SOURCE, SYS_DATA, ';
   const PKFIELDLIST = ' ID, ';
   const NUMBER_OF_PRIMARY_KEYS = 1;
   private $primaryKeyArray = array( 1 => 'ID'  ) ;
   private $allFieldsArray = array( 0 => 'ID' , 1 => 'SPECIMEN_ID' , 2 => 'BATCH_ID' , 3 => 'ACCESS_TYPE_ID' , 4 => 'SOURCE_TYPE_ID' , 5 => 'ACTIVE_FLAG' , 6 => 'DESCRIPTION' , 7 => 'CAPTION' , 8 => 'OWNER' , 9 => 'COPYRIGHT' , 10 => 'REMARKS' , 11 => 'DATA_SOURCE' , 12 => 'SYS_DATA'  ) ;
   private $selectDistinctFieldsArray = array( 1 => 'SPECIMEN_ID' , 2 => 'BATCH_ID'  ) ;

   //---------------------------------------------------------------------------

   // constructor 
   function huh_IMAGE_SET(){
       $this->ID = NULL;
       $this->SPECIMEN_ID = '';
       $this->BATCH_ID = '';
       $this->ACCESS_TYPE_ID = '';
       $this->SOURCE_TYPE_ID = '';
       $this->ACTIVE_FLAG = '';
       $this->DESCRIPTION = '';
       $this->CAPTION = '';
       $this->OWNER = '';
       $this->COPYRIGHT = '';
       $this->REMARKS = '';
       $this->DATA_SOURCE = '';
       $this->SYS_DATA = '';
       $this->dirty = false;
       $this->loaded = false;
       $this->error = '';
   }

   private function l_addslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = addslashes($value);
      }
      return $retval;
   }
   private function l_stripslashes($value) {
      $retval = $value;
      if (!get_magic_quotes_gpc()) {
          $retval = stripslashes($value);
      }
      return $retval;
   }
   public function isDirty() {
       return $this->dirty;
   }
   public function isLoaded() {
       return $this->loaded;
   }
   public function errorMessage() {
       return $this->error;
   }

   //---------------------------------------------------------------------------

   public function keyValueSet($fieldname,$value) {
       $returnvalue = false;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='ID') { $returnvalue = $this->setID($value); } 
             if ($fieldname=='SPECIMEN_ID') { $returnvalue = $this->setSPECIMEN_ID($value); } 
             if ($fieldname=='BATCH_ID') { $returnvalue = $this->setBATCH_ID($value); } 
             if ($fieldname=='ACCESS_TYPE_ID') { $returnvalue = $this->setACCESS_TYPE_ID($value); } 
             if ($fieldname=='SOURCE_TYPE_ID') { $returnvalue = $this->setSOURCE_TYPE_ID($value); } 
             if ($fieldname=='ACTIVE_FLAG') { $returnvalue = $this->setACTIVE_FLAG($value); } 
             if ($fieldname=='DESCRIPTION') { $returnvalue = $this->setDESCRIPTION($value); } 
             if ($fieldname=='CAPTION') { $returnvalue = $this->setCAPTION($value); } 
             if ($fieldname=='OWNER') { $returnvalue = $this->setOWNER($value); } 
             if ($fieldname=='COPYRIGHT') { $returnvalue = $this->setCOPYRIGHT($value); } 
             if ($fieldname=='REMARKS') { $returnvalue = $this->setREMARKS($value); } 
             if ($fieldname=='DATA_SOURCE') { $returnvalue = $this->setDATA_SOURCE($value); } 
             if ($fieldname=='SYS_DATA') { $returnvalue = $this->setSYS_DATA($value); } 
             $returnvalue = true;
          }
          catch (exception $e) { ;
              $returnvalue = false;
              throw new Exception('Field Set Error'.$e->getMessage()); 
          }
       } else { 
          throw new Exception('No Such field'); 
       }  
       return $returnvalue;
   }
   public function keyGet($fieldname) {
       $returnvalue = null;
       if ($this->hasField($fieldname)) { 
          try {
             if ($fieldname=='ID') { $returnvalue = $this->getID(); } 
             if ($fieldname=='SPECIMEN_ID') { $returnvalue = $this->getSPECIMEN_ID(); } 
             if ($fieldname=='BATCH_ID') { $returnvalue = $this->getBATCH_ID(); } 
             if ($fieldname=='ACCESS_TYPE_ID') { $returnvalue = $this->getACCESS_TYPE_ID(); } 
             if ($fieldname=='SOURCE_TYPE_ID') { $returnvalue = $this->getSOURCE_TYPE_ID(); } 
             if ($fieldname=='ACTIVE_FLAG') { $returnvalue = $this->getACTIVE_FLAG(); } 
             if ($fieldname=='DESCRIPTION') { $returnvalue = $this->getDESCRIPTION(); } 
             if ($fieldname=='CAPTION') { $returnvalue = $this->getCAPTION(); } 
             if ($fieldname=='OWNER') { $returnvalue = $this->getOWNER(); } 
             if ($fieldname=='COPYRIGHT') { $returnvalue = $this->getCOPYRIGHT(); } 
             if ($fieldname=='REMARKS') { $returnvalue = $this->getREMARKS(); } 
             if ($fieldname=='DATA_SOURCE') { $returnvalue = $this->getDATA_SOURCE(); } 
             if ($fieldname=='SYS_DATA') { $returnvalue = $this->getSYS_DATA(); } 
          }
          catch (exception $e) { ;
              $returnvalue = null;
          }
       }
       return $returnvalue;
   }
/*ID*/
   public function getID() {
       if ($this->ID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->ID));
       }
   }
   public function setID($ID) {
       if (strlen($ID) > huh_IMAGE_SET::ID_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->ID = $this->l_addslashes($ID);
       $this->dirty = true;
   }
/*SPECIMEN_ID*/
   public function getSPECIMEN_ID() {
       if ($this->SPECIMEN_ID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SPECIMEN_ID));
       }
   }
   public function setSPECIMEN_ID($SPECIMEN_ID) {
       if (strlen($SPECIMEN_ID) > huh_IMAGE_SET::SPECIMEN_ID_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->SPECIMEN_ID = $this->l_addslashes($SPECIMEN_ID);
       $this->dirty = true;
   }
/*BATCH_ID*/
   public function getBATCH_ID() {
       if ($this->BATCH_ID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->BATCH_ID));
       }
   }
   public function setBATCH_ID($BATCH_ID) {
       if (strlen($BATCH_ID) > huh_IMAGE_SET::BATCH_ID_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->BATCH_ID = $this->l_addslashes($BATCH_ID);
       $this->dirty = true;
   }
/*ACCESS_TYPE_ID*/
   public function getACCESS_TYPE_ID() {
       if ($this->ACCESS_TYPE_ID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->ACCESS_TYPE_ID));
       }
   }
   public function setACCESS_TYPE_ID($ACCESS_TYPE_ID) {
       if (strlen($ACCESS_TYPE_ID) > huh_IMAGE_SET::ACCESS_TYPE_ID_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->ACCESS_TYPE_ID = $this->l_addslashes($ACCESS_TYPE_ID);
       $this->dirty = true;
   }
/*SOURCE_TYPE_ID*/
   public function getSOURCE_TYPE_ID() {
       if ($this->SOURCE_TYPE_ID==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SOURCE_TYPE_ID));
       }
   }
   public function setSOURCE_TYPE_ID($SOURCE_TYPE_ID) {
       if (strlen($SOURCE_TYPE_ID) > huh_IMAGE_SET::SOURCE_TYPE_ID_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->SOURCE_TYPE_ID = $this->l_addslashes($SOURCE_TYPE_ID);
       $this->dirty = true;
   }
/*ACTIVE_FLAG*/
   public function getACTIVE_FLAG() {
       if ($this->ACTIVE_FLAG==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->ACTIVE_FLAG));
       }
   }
   public function setACTIVE_FLAG($ACTIVE_FLAG) {
       if (strlen(preg_replace('/[^0-9]/','',$ACTIVE_FLAG)) > huh_IMAGE_SET::ACTIVE_FLAG_SIZE) { 
           throw new Exception('Value has too many digits for the field length.');
       } 
       $ACTIVE_FLAG = trim($ACTIVE_FLAG);
       if (!ctype_digit(strval($ACTIVE_FLAG))) {
             throw new Exception("Value must be an integer");
       }
       $this->ACTIVE_FLAG = $this->l_addslashes($ACTIVE_FLAG);
       $this->dirty = true;
   }
/*DESCRIPTION*/
   public function getDESCRIPTION() {
       if ($this->DESCRIPTION==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->DESCRIPTION));
       }
   }
   public function setDESCRIPTION($DESCRIPTION) {
       if (strlen($DESCRIPTION) > huh_IMAGE_SET::DESCRIPTION_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->DESCRIPTION = $this->l_addslashes($DESCRIPTION);
       $this->dirty = true;
   }
/*CAPTION*/
   public function getCAPTION() {
       if ($this->CAPTION==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->CAPTION));
       }
   }
   public function setCAPTION($CAPTION) {
       if (strlen($CAPTION) > huh_IMAGE_SET::CAPTION_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->CAPTION = $this->l_addslashes($CAPTION);
       $this->dirty = true;
   }
/*OWNER*/
   public function getOWNER() {
       if ($this->OWNER==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->OWNER));
       }
   }
   public function setOWNER($OWNER) {
       if (strlen($OWNER) > huh_IMAGE_SET::OWNER_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->OWNER = $this->l_addslashes($OWNER);
       $this->dirty = true;
   }
/*COPYRIGHT*/
   public function getCOPYRIGHT() {
       if ($this->COPYRIGHT==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->COPYRIGHT));
       }
   }
   public function setCOPYRIGHT($COPYRIGHT) {
       if (strlen($COPYRIGHT) > huh_IMAGE_SET::COPYRIGHT_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->COPYRIGHT = $this->l_addslashes($COPYRIGHT);
       $this->dirty = true;
   }
/*REMARKS*/
   public function getREMARKS() {
       if ($this->REMARKS==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->REMARKS));
       }
   }
   public function setREMARKS($REMARKS) {
       if (strlen($REMARKS) > huh_IMAGE_SET::REMARKS_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->REMARKS = $this->l_addslashes($REMARKS);
       $this->dirty = true;
   }
/*DATA_SOURCE*/
   public function getDATA_SOURCE() {
       if ($this->DATA_SOURCE==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->DATA_SOURCE));
       }
   }
   public function setDATA_SOURCE($DATA_SOURCE) {
       if (strlen($DATA_SOURCE) > huh_IMAGE_SET::DATA_SOURCE_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->DATA_SOURCE = $this->l_addslashes($DATA_SOURCE);
       $this->dirty = true;
   }
/*SYS_DATA*/
   public function getSYS_DATA() {
       if ($this->SYS_DATA==null) { 
          return null;
       } else { ;
          return trim($this->l_stripslashes($this->SYS_DATA));
       }
   }
   public function setSYS_DATA($SYS_DATA) {
       if (strlen($SYS_DATA) > huh_IMAGE_SET::SYS_DATA_SIZE) { 
           throw new Exception('Value exceeds field length.');
       } 
       $this->SYS_DATA = $this->l_addslashes($SYS_DATA);
       $this->dirty = true;
   }
   public function PK() { // get value of primary key 
        $returnvalue = '';
        $returnvalue .= $this->getID();
        return $returnvalue;
   }
   public function PKArray() { // get name and value of primary key fields 
        $returnvalue = array();
        $returnvalue['ID'] = $this->getID();
        return $returnvalue;
   }
   public function NumberOfPrimaryKeyFields() { // returns the number of primary key fields defined for this table 
        return 1;
   }

   // Constants holding the mysqli field type character (s,i,d) for each field
  const C_IDMYSQLI_TYPE = 'i';
  const C_SPECIMEN_IDMYSQLI_TYPE = 'i';
  const C_BATCH_IDMYSQLI_TYPE = 'i';
  const C_ACCESS_TYPE_IDMYSQLI_TYPE = 'i';
  const C_SOURCE_TYPE_IDMYSQLI_TYPE = 'i';
  const C_ACTIVE_FLAGMYSQLI_TYPE = 'i';
  const C_DESCRIPTIONMYSQLI_TYPE = 's';
  const C_CAPTIONMYSQLI_TYPE = 's';
  const C_OWNERMYSQLI_TYPE = 's';
  const C_COPYRIGHTMYSQLI_TYPE = 's';
  const C_REMARKSMYSQLI_TYPE = 's';
  const C_DATA_SOURCEMYSQLI_TYPE = 's';
  const C_SYS_DATAMYSQLI_TYPE = 's';

   // function to obtain the mysqli field type character from a fieldname
   public function MySQLiFieldType($aFieldname) { 
      $retval = '';
      if ($aFieldname=='ID') { $retval = self::C_IDMYSQLI_TYPE; }
      if ($aFieldname=='SPECIMEN_ID') { $retval = self::C_SPECIMEN_IDMYSQLI_TYPE; }
      if ($aFieldname=='BATCH_ID') { $retval = self::C_BATCH_IDMYSQLI_TYPE; }
      if ($aFieldname=='ACCESS_TYPE_ID') { $retval = self::C_ACCESS_TYPE_IDMYSQLI_TYPE; }
      if ($aFieldname=='SOURCE_TYPE_ID') { $retval = self::C_SOURCE_TYPE_IDMYSQLI_TYPE; }
      if ($aFieldname=='ACTIVE_FLAG') { $retval = self::C_ACTIVE_FLAGMYSQLI_TYPE; }
      if ($aFieldname=='DESCRIPTION') { $retval = self::C_DESCRIPTIONMYSQLI_TYPE; }
      if ($aFieldname=='CAPTION') { $retval = self::C_CAPTIONMYSQLI_TYPE; }
      if ($aFieldname=='OWNER') { $retval = self::C_OWNERMYSQLI_TYPE; }
      if ($aFieldname=='COPYRIGHT') { $retval = self::C_COPYRIGHTMYSQLI_TYPE; }
      if ($aFieldname=='REMARKS') { $retval = self::C_REMARKSMYSQLI_TYPE; }
      if ($aFieldname=='DATA_SOURCE') { $retval = self::C_DATA_SOURCEMYSQLI_TYPE; }
      if ($aFieldname=='SYS_DATA') { $retval = self::C_SYS_DATAMYSQLI_TYPE; }
      return $retval;
   }

   // Function load() can take either the value of the primary key which uniquely identifies a particular row
   // or an array of array('primarykeyfieldname'=>'value') in the case of a single field primary key
   // or an array of fieldname value pairs in the case of multiple field primary key.
   public function load($pk) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        try {
             if (is_array($pk)) { 
                 $this->setID($pk[ID]);
             } else { ;
                 $this->setID($pk);
             };
        } 
        catch (Exception $e) { 
             throw new Exception($e->getMessage());
        }
        if($this->ID != NULL) {
           $sql = 'SELECT ID, SPECIMEN_ID, BATCH_ID, ACCESS_TYPE_ID, SOURCE_TYPE_ID, ACTIVE_FLAG, DESCRIPTION, CAPTION, OWNER, COPYRIGHT, REMARKS, DATA_SOURCE, SYS_DATA FROM IMAGE_SET WHERE ID = '.$this->ID ;

           $preparesql = 'SELECT ID, SPECIMEN_ID, BATCH_ID, ACCESS_TYPE_ID, SOURCE_TYPE_ID, ACTIVE_FLAG, DESCRIPTION, CAPTION, OWNER, COPYRIGHT, REMARKS, DATA_SOURCE, SYS_DATA FROM IMAGE_SET WHERE ID = ? ';

           if ($statement = $connection->prepare($preparesql)) { 
              $statement->bind_param("i", $this->ID);
              $statement->execute();
              $statement->bind_result($this->ID, $this->SPECIMEN_ID, $this->BATCH_ID, $this->ACCESS_TYPE_ID, $this->SOURCE_TYPE_ID, $this->ACTIVE_FLAG, $this->DESCRIPTION, $this->CAPTION, $this->OWNER, $this->COPYRIGHT, $this->REMARKS, $this->DATA_SOURCE, $this->SYS_DATA);
              $statement->fetch();
              $statement->close();
           }

            $this->loaded = true;
            $this->dirty = false;
        } else { 
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   // Function save() will either save the current record or insert a new record.
   // Inserts new record if the primary key field in this table is null 
   // for this instance of this object.
   // Otherwise updates the record identified by the primary key value.
   public function save() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        // Test to see if this is an insert or update.
        if ($this->ID!= NULL) {
            $sql  = 'UPDATE  IMAGE_SET SET ';
            $isInsert = false;
            $sql .=  "SPECIMEN_ID = ? ";
            $sql .=  ", BATCH_ID = ? ";
            $sql .=  ", ACCESS_TYPE_ID = ? ";
            $sql .=  ", SOURCE_TYPE_ID = ? ";
            $sql .=  ", ACTIVE_FLAG = ? ";
            $sql .=  ", DESCRIPTION = ? ";
            $sql .=  ", CAPTION = ? ";
            $sql .=  ", OWNER = ? ";
            $sql .=  ", COPYRIGHT = ? ";
            $sql .=  ", REMARKS = ? ";
            $sql .=  ", DATA_SOURCE = ? ";
            $sql .=  ", SYS_DATA = ? ";

            $sql .= "  WHERE ID = ? ";
        } else {
            $sql  = 'INSERT INTO IMAGE_SET ';
            $isInsert = true;
if ($this->PK==NULL) { throw new Exception('Can\'t insert record with null primary key for this table'); }
            $sql .= '( ID ,  SPECIMEN_ID ,  BATCH_ID ,  ACCESS_TYPE_ID ,  SOURCE_TYPE_ID ,  ACTIVE_FLAG ,  DESCRIPTION ,  CAPTION ,  OWNER ,  COPYRIGHT ,  REMARKS ,  DATA_SOURCE ,  SYS_DATA ) VALUES (';
            $sql .=  "  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .=  " ,  ? ";
            $sql .= ')';

        }
        if ($statement = $connection->prepare($sql)) { 
           if ($this->ID!= NULL ) {
              $statement->bind_param("iiiiiisssssssi", $this->ID , $this->SPECIMEN_ID , $this->BATCH_ID , $this->ACCESS_TYPE_ID , $this->SOURCE_TYPE_ID , $this->ACTIVE_FLAG , $this->DESCRIPTION , $this->CAPTION , $this->OWNER , $this->COPYRIGHT , $this->REMARKS , $this->DATA_SOURCE , $this->SYS_DATA , $this->ID );
           } else { 
              $statement->bind_param("iiiiiisssssss", $this->ID , $this->SPECIMEN_ID , $this->BATCH_ID , $this->ACCESS_TYPE_ID , $this->SOURCE_TYPE_ID , $this->ACTIVE_FLAG , $this->DESCRIPTION , $this->CAPTION , $this->OWNER , $this->COPYRIGHT , $this->REMARKS , $this->DATA_SOURCE , $this->SYS_DATA );
           } 
           $statement->execute();
           if ($statement->num_rows()!=1) {
               $this->error = $statement->error; 
           }
           $statement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }
        if ($this->error=='') { 
            $returnvalue = true;
        };

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function delete() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        if($this->ID != NULL) {
           $sql = 'SELECT ID, SPECIMEN_ID, BATCH_ID, ACCESS_TYPE_ID, SOURCE_TYPE_ID, ACTIVE_FLAG, DESCRIPTION, CAPTION, OWNER, COPYRIGHT, REMARKS, DATA_SOURCE, SYS_DATA FROM IMAGE_SET WHERE ID = "'.$this->ID.'"  ' ;

           $preparedsql = 'SELECT  FROM IMAGE_SET WHERE  and ID = ?  ' ;
        if ($statement = $connection->prepare($preparedsql)) { 
           $statement->bind_param("iiiiiisssssss", $this->ID, $this->SPECIMEN_ID, $this->BATCH_ID, $this->ACCESS_TYPE_ID, $this->SOURCE_TYPE_ID, $this->ACTIVE_FLAG, $this->DESCRIPTION, $this->CAPTION, $this->OWNER, $this->COPYRIGHT, $this->REMARKS, $this->DATA_SOURCE, $this->SYS_DATA);
           $statement->execute();
           $statement->store_result();
           if ($statement->num_rows()==1) {
                $sql = 'DELETE FROM IMAGE_SET WHERE  and ID = ?  ';
                if ($stmt_delete = $connection->prepare($sql)) { 
                   $stmt_delete->bind_param("iiiiiisssssss", $this->ID, $this->SPECIMEN_ID, $this->BATCH_ID, $this->ACCESS_TYPE_ID, $this->SOURCE_TYPE_ID, $this->ACTIVE_FLAG, $this->DESCRIPTION, $this->CAPTION, $this->OWNER, $this->COPYRIGHT, $this->REMARKS, $this->DATA_SOURCE, $this->SYS_DATA);
                   if ($stmnt_delete->execute()) { 
                       $returnvalue = true;
                   } else {
                       $this->error = mysqli_error($connection); 
                   }
                   $stmt_delete->close();
                }
           } else { 
               $this->error = mysqli_error($connection); 
           }
           $tatement->close();
        } else { 
            $this->error = mysqli_error($connection); 
        }

            $this->loaded = true;
            // record was deleted, so set PK to null
            $this->ID = NULL; 
        } else { 
           throw new Exception('Unable to identify which record to delete, primary key is not set');
        }
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function count() {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = false;
        $sql = 'SELECT count(*)  FROM IMAGE_SET';
        if ($result = $connection->query($sql)) { 
           if ($result->num_rows()==1) {
             $row = $result->fetch_row();
             if ($row) {
                $returnvalue = $row[0];
             }
           }
        } else { 
           $this->error = mysqli_error($connection); 
        }
        mysqli_free_result($result);

        $this->loaded = true;
        return $returnvalue;
    }
   //---------------------------------------------------------------------------

   public function loadArrayKeyValueSearch($searchTermArray) {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = array();
       $and = '';
       $wherebit = 'WHERE ';
       foreach($searchTermArray as $fieldname => $searchTerm) {
           if ($this->hasField($fieldname)) { 
               $operator = '='; 
               // change to a like search if a wildcard character is present
               if (!(strpos($searchTerm,'%')===false)) { $operator = 'like'; }
               if (!(strpos($searchTerm,'_')===false)) { $operator = 'like'; }
               if ($searchTerm=='[NULL]') { 
                   $wherebit .= "$and ($fieldname is null or $fieldname='') "; 
               } else { 
                   $wherebit .= "$and $fieldname $operator ? ";
                   $types = $types . $this->MySQLiFieldType($fieldname);
               } 
               $and = ' and ';
           }
       }
       $sql = "SELECT ID FROM IMAGE_SET $wherebit";
       if ($wherebit=='') { 
             $this->error = 'Error: No search terms provided';
       } else {
          $statement = $connection->prepare($sql);
          $vars = Array();
          $vars[] = $types;
          $i = 0;
          foreach ($searchTermArray as $value) { 
               $varname = 'bind'.$i;  // create a variable name
               $$varname = $value;    // using that variable name store the value 
               $vars[] = &$$varname;  // add a reference to the variable to the array
               $i++;
           }
           //$vars[] contains $types followed by references to variables holding each value in $searchTermArray.
          call_user_func_array(array($statement,'bind_param'),$vars);
          //$statement->bind_param($types,$names);
          $statement->execute();
          $statement->bind_result($id);
          $ids = array();
          while ($statement->fetch()) {
              $ids[] = $id;
          } // double loop to allow all data to be retrieved before preparing a new statement. 
          $statement->close();
          for ($i=0;$i<count($ids);$i++) {
              $obj = new huh_IMAGE_SET();
              $obj->load($ids[$i]);
              $returnvalue[] = $obj;
              $result=true;
          }
          if ($result===false) { $this->error = mysqli_error($connection); }
       }
       return $returnvalue;
   }	

   //---------------------------------------------------------------------------

// TODO: *************** link to related tables 

   //---------------------------------------------------------------------------

   // Returns an array of primary key values (id) and concatenated values of all other fields (fields)
   // wrapped as $values in: '{ "identifier":"id", "items": [ '.$values.' ] }';
   // when druid_handler.php is called with druid_action=returnfkjson
   // Used with dojo dijit.form.FilteringSelect to submit surrogate numeric key values from a text picklist 
   public function keySelectAllConcatJSON($orderby='ASC') {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
       $returnvalue = '';
       $order = '';
       if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
       $sql = "SELECT ID, concat(IFNULL(SPECIMEN_ID,'') || ' ' || IFNULL(BATCH_ID,'') || ' ' || IFNULL(ACCESS_TYPE_ID,'') || ' ' || IFNULL(SOURCE_TYPE_ID,'') || ' ' || IFNULL(ACTIVE_FLAG,'') || ' ' || IFNULL(DESCRIPTION,'') || ' ' || IFNULL(CAPTION,'') || ' ' || IFNULL(OWNER,'') || ' ' || IFNULL(COPYRIGHT,'') || ' ' || IFNULL(REMARKS,'') || ' ' || IFNULL(DATA_SOURCE,'') || ' ' || IFNULL(SYS_DATA,'')) FROM IMAGE_SET order by SPECIMEN_ID, BATCH_ID $order ";
       $comma = '';
       if ($result = $connection->query($sql)) { 
          while ($row = $result->fetch_row()) {
             if ($row) {
                $pkval = trim($row[0]);
                $fval = trim($row[1]);
                if ($pkval!='') { 
                    $pkval = str_replace('"','&quot;',$pkval);
                    $fval = str_replace('"','&quot;',$fval);
                    $returnvalue .= $comma . ' { "id":"'.$pkval.'", "fields": "'.$fval.'" } ';
                    $comma = ', ';
                }
             }
          }
       } else { 
          $this->error = mysqli_error(); 
       }
       $result->close();
       return $returnvalue;
   }
   //---------------------------------------------------------------------------

   public function keySelectDistinctJSON($field,$orderby='ASC') {
       // ******* Note: $connection must be a mysqli object.
       global $connection;
       $returnvalue = '';
       if ($this->hasField($field)) { 
          $order = '';
          $fieldesc = mysql_escape_string($field);
          if ($orderby=='ASC') { $order = 'ASC'; } else { $order = 'DESC'; } 
          $preparemysql = "SELECT DISTINCT $fieldesc FROM IMAGE_SET order by $fieldesc $order ";
          $comma = '';
         if ($stmt = $connection->prepare($preparemysql)) { 
             $stmt->execute();
             $stmt->bind_result($val);
             while ($stmt->fetch()) {
                $val = trim($val);                if ($val!='') { 
                    $val = str_replace('"','&quot;',$val);
                    $returnvalue .= $comma . ' { "'.$field.'":"'.$val.'" } ';
                    $comma = ', ';
                 }
             }
             $stmt->close();
         }
       }
       return $returnvalue;
   }
   //---------------------------------------------------------------------------


   //---------------------------------------------------------------------------

   // Each field with an index has a load array method generated for it.
   public function loadArrayBySPECIMEN_ID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT ID FROM IMAGE_SET WHERE SPECIMEN_ID $operator '$searchTerm'";
        $preparedsql = "SELECT ID FROM IMAGE_SET WHERE SPECIMEN_ID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_IMAGE_SET();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }
   public function loadArrayByBATCH_ID($searchTerm) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = array();
        $operator = "=";
        // change to a like search if a wildcard character is present
        if (!(strpos($searchTerm,"%")===false)) { $operator = "like"; }
        if (!(strpos($searchTerm,"_")===false)) { $operator = "like"; }
        $sql = "SELECT ID FROM IMAGE_SET WHERE BATCH_ID $operator '$searchTerm'";
        $preparedsql = "SELECT ID FROM IMAGE_SET WHERE BATCH_ID $operator ? ";
        if ($statement = $connection->prepare($preparedsql)) { 
            $statement->bind_param("s", $searchTerm);
            $statement->execute();
            $statement->bind_result($id);
            while ($statement->fetch()) { ;
                $obj = new huh_IMAGE_SET();
                $obj->load($id);
                $returnvalue[] = $obj;
            }
            $statement->close();
        }
        return $returnvalue;
   }

   //---------------------------------------------------------------------------

   // Each fulltext index has a load array method generated for it.

   //---------------------------------------------------------------------------

   // Each field with an index has a select distinct method generated for it.
   public function selectDistinctSPECIMEN_ID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' SPECIMEN_ID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, SPECIMEN_ID FROM IMAGE_SET group by SPECIMEN_ID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&SPECIMEN_ID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }
   public function selectDistinctBATCH_ID($startline,$link,$endline,$includecount=false,$orderbycount=false) {
        // ******* Note: $connection must be a mysqli object.
        global $connection;
        $returnvalue = '';
        $order = ' BATCH_ID ';
        if ($orderbycount) { $order = ' druid_ct DESC '; } 
        $sql = "SELECT count(*) as druid_ct, BATCH_ID FROM IMAGE_SET group by BATCH_ID order by $order ";
        if ($result = $connection->query($sql)) { 
           while ($row = $result->fetch_row()) {
              if ($row) {
                  $count = $row[0];
                  $val = $row[1];
                  $escaped = urlencode($row[1]);
                  if ($val=='') {
                     $val = '[NULL]'; 
                     $escaped = urlencode('[NULL]');
                  }
                  if ($link=='') { 
                     $returnvalue .= "$startline$val&nbsp;($count)$endline";
                  } else { 
                     $returnvalue .= "$startline<a href='$link&BATCH_ID=$escaped'>$val</a>&nbsp;($count)$endline";
                  }
              }
           }
           $result->close();
        }
        return $returnvalue;
    }

   public function keySelectDistinct($fieldname,$startline,$link,$endline,$includecount=false,$orderbycount=false) {
       $returnvalue = '';
       switch ($fieldname) { 
          case 'SPECIMEN_ID':
             $returnvalue = $this->selectDistinctSPECIMEN_ID($startline,$link,$endline,$includecount,$orderbycount);
             break;
          case 'BATCH_ID':
             $returnvalue = $this->selectDistinctBATCH_ID($startline,$link,$endline,$includecount,$orderbycount);
             break;
       }
       return $returnvalue;
    }

   //---------------------------------------------------------------------------

   public function hasField($fieldname) {
       $returnvalue = false;
       if (trim($fieldname)!='' && trim($fieldname)!=',') {
            if (strpos(self::FIELDLIST," $fieldname, ")!==false) { 
               $returnvalue = true;
            }
       }
       return $returnvalue;
    }
   //---------------------------------------------------------------------------

}


// Write your own views by extending this class.
// Place your extended views in a separate file and you
// can use Druid to regenerate the IMAGE_SET.php file to reflect changes
// in the underlying database without overwriting your custom views of the data.
// 
class huh_IMAGE_SETView implements viewer
{
   var $model = null;
   public function setModel($aModel) { 
       $this->model = $aModel;
   }
   // @param $includeRelated default true shows rows from other tables through foreign key relationships.
   // @param $editLinkURL default '' allows adding a link to show this record in an editing form.
   public function getDetailsView($includeRelated=true, $editLinkURL='') {
       $returnvalue = '<ul>';
       $editLinkURL=trim($editLinkURL);
       $model = $this->model;
       $primarykeys = $model->schemaPK();
       if ($editLinkURL!='') { 
          if (!preg_match('/\&$/',$editLinkURL)) { $editLinkURL .= '&'; } 
          $nullpk = false; 
          foreach ($primarykeys as $primarykey) { 
              // Add fieldname=value pairs for primary key(s) to editLinkURL. 
              $editLinkURL .= urlencode($primarykey) . '=' . urlencode($model->keyGet($primarykey));
              if ($model->keyGet($primarykey)=='') { $nullpk = true; } 
          }
          if (!$nullpk) { $returnvalue .= "<li>huh_IMAGE_SET <a href='$editLinkURL'>Edit</a></li>\n";  } 
       }
       $returnvalue .= "<li>".huh_IMAGE_SET::ID.": ".$model->getID()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::SPECIMEN_ID.": ".$model->getSPECIMEN_ID()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::BATCH_ID.": ".$model->getBATCH_ID()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::ACCESS_TYPE_ID.": ".$model->getACCESS_TYPE_ID()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::SOURCE_TYPE_ID.": ".$model->getSOURCE_TYPE_ID()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::ACTIVE_FLAG.": ".$model->getACTIVE_FLAG()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::DESCRIPTION.": ".$model->getDESCRIPTION()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::CAPTION.": ".$model->getCAPTION()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::OWNER.": ".$model->getOWNER()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::COPYRIGHT.": ".$model->getCOPYRIGHT()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::REMARKS.": ".$model->getREMARKS()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::DATA_SOURCE.": ".$model->getDATA_SOURCE()."</li>\n";
       $returnvalue .= "<li>".huh_IMAGE_SET::SYS_DATA.": ".$model->getSYS_DATA()."</li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.

        }
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getJSON() {
       $returnvalue = '{ ';
       $model = $this->model;
       $returnvalue .= '"'.huh_IMAGE_SET::ID.': "'.$model->getID().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::SPECIMEN_ID.': "'.$model->getSPECIMEN_ID().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::BATCH_ID.': "'.$model->getBATCH_ID().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::ACCESS_TYPE_ID.': "'.$model->getACCESS_TYPE_ID().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::SOURCE_TYPE_ID.': "'.$model->getSOURCE_TYPE_ID().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::ACTIVE_FLAG.': "'.$model->getACTIVE_FLAG().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::DESCRIPTION.': "'.$model->getDESCRIPTION().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::CAPTION.': "'.$model->getCAPTION().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::OWNER.': "'.$model->getOWNER().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::COPYRIGHT.': "'.$model->getCOPYRIGHT().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::REMARKS.': "'.$model->getREMARKS().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::DATA_SOURCE.': "'.$model->getDATA_SOURCE().'",';
       $returnvalue .= '"'.huh_IMAGE_SET::SYS_DATA.': "'.$model->getSYS_DATA().'" }';
       $returnvalue .= '</ul>';
       return  $returnvalue;
   }
   public function getTableRowView() {
       $returnvalue = '<tr>';
       $model = $this->model;
       $returnvalue .= "<td>".$model->getID()."</td>\n";
       $returnvalue .= "<td>".$model->getSPECIMEN_ID()."</td>\n";
       $returnvalue .= "<td>".$model->getBATCH_ID()."</td>\n";
       $returnvalue .= "<td>".$model->getACCESS_TYPE_ID()."</td>\n";
       $returnvalue .= "<td>".$model->getSOURCE_TYPE_ID()."</td>\n";
       $returnvalue .= "<td>".$model->getACTIVE_FLAG()."</td>\n";
       $returnvalue .= "<td>".$model->getDESCRIPTION()."</td>\n";
       $returnvalue .= "<td>".$model->getCAPTION()."</td>\n";
       $returnvalue .= "<td>".$model->getOWNER()."</td>\n";
       $returnvalue .= "<td>".$model->getCOPYRIGHT()."</td>\n";
       $returnvalue .= "<td>".$model->getREMARKS()."</td>\n";
       $returnvalue .= "<td>".$model->getDATA_SOURCE()."</td>\n";
       $returnvalue .= "<td>".$model->getSYS_DATA()."</td>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getHeaderRow() {
       $returnvalue = '<tr>';
       $returnvalue .= "<th>".huh_IMAGE_SET::ID."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::SPECIMEN_ID."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::BATCH_ID."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::ACCESS_TYPE_ID."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::SOURCE_TYPE_ID."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::ACTIVE_FLAG."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::DESCRIPTION."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::CAPTION."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::OWNER."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::COPYRIGHT."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::REMARKS."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::DATA_SOURCE."</th>\n";
       $returnvalue .= "<th>".huh_IMAGE_SET::SYS_DATA."</th>\n";
       $returnvalue .= '</tr>';
       return  $returnvalue;
   }
   public function getEditFormDojoView($includeRelated=true) {
       $model = $this->model;
       if ($model->PK()=='') { $addform=true; } else { $addform=false; } 
       $returnvalue = '';
       $id = trim($model->PK());
       $feedback = "status$id";
       $formname = "editform$id";
       $script = '
  var saveprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function save'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Saving...";
     dojo.xhrGet(saveprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_IMAGE_SET">';
       $returnvalue .= "<div id='div_$formnane' >";
       if($addform) { 
          $returnvalue .= "Add a new ID"; 
       } else { 
         $returnvalue .= "<div><label for=".huh_IMAGE_SET::ID.">ID</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::ID." id=".huh_IMAGE_SET::ID." value='".$model->getID()."'  style=' width:".huh_IMAGE_SET::ID_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::ID_SIZE ."' ></div>\n";
       }  
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::SPECIMEN_ID.">SPECIMEN_ID</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::SPECIMEN_ID." id=".huh_IMAGE_SET::SPECIMEN_ID." value='".$model->getSPECIMEN_ID()."'  style=' width:".huh_IMAGE_SET::SPECIMEN_ID_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::SPECIMEN_ID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::BATCH_ID.">BATCH_ID</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::BATCH_ID." id=".huh_IMAGE_SET::BATCH_ID." value='".$model->getBATCH_ID()."'  style=' width:".huh_IMAGE_SET::BATCH_ID_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::BATCH_ID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::ACCESS_TYPE_ID.">ACCESS_TYPE_ID</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::ACCESS_TYPE_ID." id=".huh_IMAGE_SET::ACCESS_TYPE_ID." value='".$model->getACCESS_TYPE_ID()."'  style=' width:".huh_IMAGE_SET::ACCESS_TYPE_ID_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::ACCESS_TYPE_ID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::SOURCE_TYPE_ID.">SOURCE_TYPE_ID</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::SOURCE_TYPE_ID." id=".huh_IMAGE_SET::SOURCE_TYPE_ID." value='".$model->getSOURCE_TYPE_ID()."'  style=' width:".huh_IMAGE_SET::SOURCE_TYPE_ID_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::SOURCE_TYPE_ID_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::ACTIVE_FLAG.">ACTIVE_FLAG</label><input  dojoType='dijit.form.ValidationTextBox'  regExp='^[0-9-]*'  invalidMessage='The value entered is not valid.  It must be an integer.' name=".huh_IMAGE_SET::ACTIVE_FLAG." id=".huh_IMAGE_SET::ACTIVE_FLAG." value='".$model->getACTIVE_FLAG()."'  style=' width:".huh_IMAGE_SET::ACTIVE_FLAG_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::ACTIVE_FLAG_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::DESCRIPTION.">DESCRIPTION</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_IMAGE_SET::DESCRIPTION." id=".huh_IMAGE_SET::DESCRIPTION." >".$model->getDESCRIPTION()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::CAPTION.">CAPTION</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_IMAGE_SET::CAPTION." id=".huh_IMAGE_SET::CAPTION." >".$model->getCAPTION()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::OWNER.">OWNER</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_IMAGE_SET::OWNER." id=".huh_IMAGE_SET::OWNER." >".$model->getOWNER()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::COPYRIGHT.">COPYRIGHT</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_IMAGE_SET::COPYRIGHT." id=".huh_IMAGE_SET::COPYRIGHT." >".$model->getCOPYRIGHT()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::REMARKS.">REMARKS</label><textarea  dojoType='dijit.form.Textarea'  style=' width:51em; border:1px solid grey; '  name=".huh_IMAGE_SET::REMARKS." id=".huh_IMAGE_SET::REMARKS." >".$model->getREMARKS()."</textarea></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::DATA_SOURCE.">DATA_SOURCE</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::DATA_SOURCE." id=".huh_IMAGE_SET::DATA_SOURCE." value='".$model->getDATA_SOURCE()."'  style=' width:".huh_IMAGE_SET::DATA_SOURCE_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::DATA_SOURCE_SIZE ."' ></div>\n";
       $returnvalue .= "<div><label for=".huh_IMAGE_SET::SYS_DATA.">SYS_DATA</label><input  dojoType='dijit.form.ValidationTextBox'  name=".huh_IMAGE_SET::SYS_DATA." id=".huh_IMAGE_SET::SYS_DATA." value='".$model->getSYS_DATA()."'  style=' width:".huh_IMAGE_SET::SYS_DATA_SIZE ."em;  '  maxlength='".huh_IMAGE_SET::SYS_DATA_SIZE ."' ></div>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= "<li><input name=save id=save type=button value='Save' onclick='$script  save$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</div>';
       $returnvalue .= '</form>';
       if(!$addform) { 
          // show delete button if editing an existing record 
       $id = trim($model->PK());
       $feedback = "deletestatus$id";
       $formname = "deleteform$id";
       $script = '
  var deleteprocessor'.$id.' = {
        form: dojo.byId("'.$formname.'"),
        url: "ajax_handler.php",
        handleAs: "text",
        load: function(data){
                dojo.byId("'.$feedback.'").innerHTML = data;
        },
        error: function(data){
                dojo.byId("'.$feedback.'").innerHTML = "Error: " + data;
                console.debug("Error: ", data);
        },
        timeout: 8000
  };
  function deleterecord'.$id.'() {
     dojo.byId("'.$feedback.'").innerHTML = "Deleting...";
     dojo.xhrGet(deleteprocessor'.$id.');
  };
       ';
       $returnvalue .= "<form id='$formname' name='$formname' dojoType='dijit.form.Form'>";
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=delete>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_table value="huh_IMAGE_SET">';
       $returnvalue .= '<input type=hidden name=ID id=druid_table value="'.$id.'">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li><input name=delete id=save type=button value='Delete' onclick='$script  deleterecord$id();'><div id='$feedback'></div></li>";
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       } 
       return  $returnvalue;
   }
   public function getEditFormView($includeRelated=true) {
       $model = $this->model;
       $returnvalue = '<form method=get action=druid_handler.php>';
       $returnvalue .= '<input type=hidden name=druid_action id=druid_action value=save>';
       $returnvalue .= '<input type=hidden name=druid_table id=druid_action value="huh_IMAGE_SET">';
       $returnvalue .= '<ul>';
       $returnvalue .= "<li>ID<input type=text name=".huh_IMAGE_SET::ID." id=".huh_IMAGE_SET::ID." value='".$model->getID()."'  size='".huh_IMAGE_SET::ID_SIZE ."'  maxlength='".huh_IMAGE_SET::ID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>SPECIMEN_ID<input type=text name=".huh_IMAGE_SET::SPECIMEN_ID." id=".huh_IMAGE_SET::SPECIMEN_ID." value='".$model->getSPECIMEN_ID()."'  size='".huh_IMAGE_SET::SPECIMEN_ID_SIZE ."'  maxlength='".huh_IMAGE_SET::SPECIMEN_ID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>BATCH_ID<input type=text name=".huh_IMAGE_SET::BATCH_ID." id=".huh_IMAGE_SET::BATCH_ID." value='".$model->getBATCH_ID()."'  size='".huh_IMAGE_SET::BATCH_ID_SIZE ."'  maxlength='".huh_IMAGE_SET::BATCH_ID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>ACCESS_TYPE_ID<input type=text name=".huh_IMAGE_SET::ACCESS_TYPE_ID." id=".huh_IMAGE_SET::ACCESS_TYPE_ID." value='".$model->getACCESS_TYPE_ID()."'  size='".huh_IMAGE_SET::ACCESS_TYPE_ID_SIZE ."'  maxlength='".huh_IMAGE_SET::ACCESS_TYPE_ID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>SOURCE_TYPE_ID<input type=text name=".huh_IMAGE_SET::SOURCE_TYPE_ID." id=".huh_IMAGE_SET::SOURCE_TYPE_ID." value='".$model->getSOURCE_TYPE_ID()."'  size='".huh_IMAGE_SET::SOURCE_TYPE_ID_SIZE ."'  maxlength='".huh_IMAGE_SET::SOURCE_TYPE_ID_SIZE ."' ></li>\n";
       $returnvalue .= "<li>ACTIVE_FLAG<input type=text name=".huh_IMAGE_SET::ACTIVE_FLAG." id=".huh_IMAGE_SET::ACTIVE_FLAG." value='".$model->getACTIVE_FLAG()."'  size='".huh_IMAGE_SET::ACTIVE_FLAG_SIZE ."'  maxlength='".huh_IMAGE_SET::ACTIVE_FLAG_SIZE ."' ></li>\n";
       $returnvalue .= "<li>DESCRIPTION<input type=text name=".huh_IMAGE_SET::DESCRIPTION." id=".huh_IMAGE_SET::DESCRIPTION." value='".$model->getDESCRIPTION()."'  size='51'  maxlength='".huh_IMAGE_SET::DESCRIPTION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>CAPTION<input type=text name=".huh_IMAGE_SET::CAPTION." id=".huh_IMAGE_SET::CAPTION." value='".$model->getCAPTION()."'  size='51'  maxlength='".huh_IMAGE_SET::CAPTION_SIZE ."' ></li>\n";
       $returnvalue .= "<li>OWNER<input type=text name=".huh_IMAGE_SET::OWNER." id=".huh_IMAGE_SET::OWNER." value='".$model->getOWNER()."'  size='51'  maxlength='".huh_IMAGE_SET::OWNER_SIZE ."' ></li>\n";
       $returnvalue .= "<li>COPYRIGHT<input type=text name=".huh_IMAGE_SET::COPYRIGHT." id=".huh_IMAGE_SET::COPYRIGHT." value='".$model->getCOPYRIGHT()."'  size='51'  maxlength='".huh_IMAGE_SET::COPYRIGHT_SIZE ."' ></li>\n";
       $returnvalue .= "<li>REMARKS<input type=text name=".huh_IMAGE_SET::REMARKS." id=".huh_IMAGE_SET::REMARKS." value='".$model->getREMARKS()."'  size='51'  maxlength='".huh_IMAGE_SET::REMARKS_SIZE ."' ></li>\n";
       $returnvalue .= "<li>DATA_SOURCE<input type=text name=".huh_IMAGE_SET::DATA_SOURCE." id=".huh_IMAGE_SET::DATA_SOURCE." value='".$model->getDATA_SOURCE()."'  size='".huh_IMAGE_SET::DATA_SOURCE_SIZE ."'  maxlength='".huh_IMAGE_SET::DATA_SOURCE_SIZE ."' ></li>\n";
       $returnvalue .= "<li>SYS_DATA<input type=text name=".huh_IMAGE_SET::SYS_DATA." id=".huh_IMAGE_SET::SYS_DATA." value='".$model->getSYS_DATA()."'  size='".huh_IMAGE_SET::SYS_DATA_SIZE ."'  maxlength='".huh_IMAGE_SET::SYS_DATA_SIZE ."' ></li>\n";
       if ($includeRelated) { 
           // note that $includeRelated is provided as false in calls out to
           // related tables to prevent infinite loops between related objects.
        }
       $returnvalue .= '<li><input type=submit value="Save"></li>';
       $returnvalue .= '</ul>';
       $returnvalue .= '</form>';
       return  $returnvalue;
   }
}

//==============================================================================
?>
